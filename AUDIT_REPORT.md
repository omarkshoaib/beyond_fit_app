# Beyond Fit — Full Module Audit Report

Generated by 43-agent read-only audit (11 subsystem auditors + adversarial verification of every high/critical finding against live code).

**Totals:** 107 findings — 🔴 4 critical, 🟠 28 high, 🟡 46 medium, ⚪ 29 low.

By category: data-quality=22, correctness=19, bug=19, security=16, test-gap=13, usability=9, content-gap=9

Baseline: 230 tests pass, 179 exercises, ~64 foods. CLAUDE.md known-gaps list is stale (verified — claimed test failures do not occur).

---

## ⭐ Confirmed critical + high (adversarially verified REAL)

### Workout generation engine
- 🟠 **HIGH** `app/generator.py:93-96` — **Powerlifter 5/6-day splits ignore avatar, collapse to thin days**
  - resolve_split returns a generic split (5d Upper/Lower/Push/Pull/Legs, 6d PPL/PPL) regardless of avatar; only 30 of 179 exercises are powerlifter-tagged, so advanced powerlifter 5-day gives Lower 2 slots, Legs 2 slots, and Upper/Push/Pull each end with Barbell Shrug as the only surviving isolation.
  - **Fix:** Add powerlifter-specific 5/6-day templates and expand powerlifter avatar_tags, or allow powerbuilder accessories for powerlifters.
  - _Verifier note:_ Suggested fix is correct in direction but the load-bearing component must be the pool fix, not the split-branching. I empirically tested the discriminator the advisor flagged: powerlifter 3-day and 4-day splits ALREADY honor avatar and route to dedicated templates (Squat_Day/Bench_Day/Deadlift_Day/Accessory_GPP) -- yet they are ALSO thin (2-3 slots/day, Barbell Shrug recurring; 4-day Accessory/GPP = [Pull-Up, Shrug]). This proves that merely adding powerlifter-specific 5/6-day templates and branching _resolve_split will NOT fix the degenerate output, because the binding constraint is the narrow powerlifter exercise pool (only 4 low-fatigue exercises, 3 of them core/lower_back). RECOMMENDED FIX, in priority order: (1) Expand the powerlifter exercise pool, especially isolations/accessories across chest, back, shoulders, arms, quads, hams, calves (add powerlifter to many existing powerbuilder-tagged accessories in exercise_db.py); OR (2) allow powerbuilder-tagged accessories/isolations to fill powerlifter accessory+isolation slots -- e.g. in _filter_exercises (generator.py:139) treat 'powerlifter' as also accepting 'powerbuilder' exercises for non-main slots, while keeping powerlifter-specific competition-lift selection for the main_compound. Adding the 5/6-day powerlifter templates + _resolve_split branch is still worthwhile for structure (so 6-day stops duplicating PPL into single-exercise days) but is secondary and insufficient alone.
- 🟠 **HIGH** `app/generator.py:416-419` — **Weekly volume budget spent greedily in day order, starving repeated day-types**
  - budget is built once per week (line 471) and _spend_budget mutates it across all days in order, so when a day-type repeats (6-day PPL, 3-day FBA-twice) later occurrences get leftovers. Traced 6-day beginner: Push#1 5 slots/16 sets but Push#2 4 slots/11 sets; upper_chest accessory cut 3 to 1 set and side_delts isolation dropped (line 417). The two Push days are also identical exercises.
  - **Fix:** Divide each muscle weekly cap by the number of days training it before filling, or trim evenly in a post-pass.
  - _Verifier note:_ Directionally correct but needs refinement. "Divide each muscle weekly cap by the number of days training it before filling" UNDER-fills every occurrence when a single day's template demand for that muscle exceeds cap/days: e.g. a Push day wants chest = 4 (bench) + 3 (incline) = 7, but beginner cap 12 / 2 Push days = 6, so BOTH Push days would now truncate to 6 instead of one getting the full 7. The division only behaves well when per-day demand <= cap/days. Cleaner options: (a) trim evenly in a post-pass over all days after filling, or (b) reset/allocate a fresh per-day sub-budget so no single day can be starved by an earlier one. Root cause to call out: the weekly per-muscle cap (12 for beginner) is simply below the summed template demand across repeated day-types, so any fix redistributes the scarcity rather than eliminating it — the real cure is to either raise beginner caps to cover the chosen split or scale template set counts to the number of times a day repeats.
- 🟠 **HIGH** `app/generator.py:30-46` — **AutoRegulator.calculate_next_load is uncapped and live, up to +20pct weekly jumps**
  - 4pct/RPE error correction plus 2.5pct/RPE stimulus with no clamp; RPE error -4 returns last_weight*1.20 (verified 100 to 120kg). Runs in _fill_slots (line 364) when prior_week exists and bot.py:465 calls generate(profile, prior_workout) on check-in, so the uncapped path is production-live. The parallel derive_plan_delta (autoregulation.py:127,134) caps the same correction at +/-10pct; inconsistent caps.
  - **Fix:** Clamp calculate_next_load to 0.90-1.10x last_weight to match derive_plan_delta, and consolidate the two engines.
  - _Verifier note:_ Clamping calculate_next_load's return to 0.90-1.10x last_weight is the correct, load-bearing fix and directly matches derive_plan_delta's ±10% caps. Implement by clamping before the round-to-2.5 step, e.g. next_target = min(max(next_target, 0.90*last_weight), 1.10*last_weight) prior to round(next_target/2.5)*2.5. Note the cap should be relative to last_weight (the original), not corrected_baseline, to truly bound the weekly jump. The 'consolidate the two engines' recommendation is valid and arguably necessary: the capped engine (derive_plan_delta load multipliers / apply_delta / checkin_service.apply_autoregulation) is effectively dead for load in the bot, so the two engines currently disagree silently — but that consolidation is a larger refactor; the clamp alone removes the immediate safety hazard. Also worth tightening: rpe range allows actual RPE as low as 1, so even a clamp at 0.90 still permits the max -1RPE-point sized backoff to be capped correctly; verify the clamp also bounds the positive-error (too-hard) backoff symmetrically.

### Nutrition domain + service
- 🔴 **CRITICAL** `app/domain/nutrition/meal_builder.py:102-103` — **Religious-restriction filter is inert -> halal/kosher clients served pork**
  - filter_food_pool excludes a food only if a requested restriction string is FOUND in that food's religious_restrictions list: `[f for f in result if not any(r in f.religious_restrictions for r in religious_restrictions)]`. But in food_db.py NOT A SINGLE FoodItem populates religious_restrictions (every entry passes `[]`). Verified empirically: filter_food_pool(pool, religious_restrictions=['halal']) returns all 68 foods INCLUDING 'Pork Tenderloin'. The semantics are also inverted vs intent: foods should be tagged with what they VIOLATE (e.g. pork tagged 'halal','kosher'), but the data is empty so the filter is a no-op for every religious client. A Muslim or Jewish paying client receives a plan containing pork/shellfish.
  - **Fix:** Populate religious_restrictions on non-compliant foods (pork_tenderloin -> ['halal','kosher'], shellfish/shrimp -> ['kosher'], etc.) so the existing exclusion logic fires; add a test asserting no halal-violating food survives the filter. Long term tag every food with explicit halal/kosher compliance.
  - _Verifier note:_ Fix direction (populate the data) is correct, but correct one piece of the finding's framing: the filter LOGIC at line 103 is NOT inverted — it is correct as written (exclude a food when a requested restriction is found in that food's violate-list). The ONLY defect is empty data. Do NOT flip/rewrite the comprehension; flipping it would break a currently-correct filter. DATA-ONLY fix. Concrete violators in this 68-item DB (grep found no bacon/ham/lard/wine/gelatin, only these two): pork_tenderloin -> religious_restrictions=['halal','kosher']; shrimp -> ['kosher'] (shellfish is non-kosher; in mainstream interpretation shrimp is generally permitted as halal, so tagging it 'halal' would be over-restrictive — tag kosher only). Add a regression test asserting no halal-violating and no kosher-violating food survives filter_food_pool. Note an unrelated, harmless out-of-scope data typo in chicken_breast diet_tags (`"vegan"[1:]+"egan"[1:][:0]` yields "egan" not "vegan") — separate finding, does not affect this one.
- 🔴 **CRITICAL** `app/services/nutrition_service.py:86-115` — **Hypertension/type2_diabetes clients get a persisted 7-day 0-kcal plan**
  - filter_food_pool (meal_builder.py:109-114) requires 'low_sodium' in f.medical_tags for hypertension and 'low_sugar' in f.medical_tags for type2_diabetes. NO food in food_db.py sets medical_tags (it defaults to []). Verified: filter_food_pool(pool, medical_conditions=['hypertension']) returns 0 foods; same for type2_diabetes. The hard filter empties the pool and the soft-filter rescue at meal_builder.py:123-125 does not help (the pool is already empty). nutrition_service.generate has NO `if not filtered_pool` guard: it loops build_day_plan 7 times over the empty pool, which returns DayPlan(kcal=0, protein_g=0, slots with 0 items) (verified), then persists a NutritionPlan whose plan_json is 7 days of zero-calorie empty slots while kcal_target/protein_g still show the computed targets. validate_day is imported (line 21) but NEVER called, so nothing catches it. The client is shown an activatable plan of literally no food.
  - **Fix:** Tag foods with medical_tags (low_sodium/low_sugar) so the filter has data, OR change the medical filter to a soft/degrade-gracefully step. In nutrition_service.generate add `if not filtered_pool: logger.error(...); return None` and surface a clear message; additionally call validate_day per day and refuse/flag plans that fail.
  - _Verifier note:_ The suggested fix is directionally correct but the `if not filtered_pool: return None` guard ALONE is insufficient and would itself be a bug: it would leave every hypertensive/diabetic user permanently unable to get ANY plan, and the bot would then show the misleading "profile may be incomplete. Please try /diet again." message (bot.py:3469) — which is false, the profile is complete. The PRIMARY fix is the tagging-or-soft-filter half: either (a) tag foods with low_sodium/low_sugar medical_tags so the filter has data, or (b) make the medical filter degrade gracefully — mirror the existing soft-filter rescue pattern at meal_builder.py:123-125 (keep the unfiltered hard-pool when the medical filter would empty it, and log a warning). The `if not filtered_pool` guard and calling validate_day per day are good BACKSTOPS, not the primary fix. Recommend (b) as the cleanest minimal change since it reuses the established degrade-gracefully convention already in the same function.
- 🟠 **HIGH** `app/domain/nutrition/macros.py:29-45, 73-93` — **Keto is only a food filter, never a macro target -> impossible-to-hit ~160g carbs on a zero-grain pool**
  - diet_style='keto' filters foods to keto-tagged items (verified pool has 31 foods, ZERO grains). But calculate_macros derives carbs purely from goal (fat_loss/maintain/etc.), never from diet_style, and keto is not a goal key. A keto client with goal='maintain' at 1800 kcal/80kg gets protein_g=144, fat_g=64, carb_g=162 (verified). The meal builder must then hit 162g carbs from a pool with no starches, which is physically impossible, so every keto day badly misses macros. Because validate_day is never called, the broken plan ships. Keto/low-carb has no calorie-from-fat re-weighting at all.
  - **Fix:** Add a keto/low-carb macro path: when diet_style in ('keto',) cap carbs (e.g. <=50g or <=5% kcal) and reallocate remaining kcal to fat. Make goal and diet_style jointly drive macros, and validate that the filtered pool can supply the carb target before shipping.
  - _Verifier note:_ Fix is correct and self-consistent. Add a keto/low-carb macro path: when diet_style in ('keto',) (and ideally 'low_carb'), cap carbs (e.g. <=50g absolute or <=5% kcal) and reallocate the freed kcal to fat at the kcal level so total target_kcal is held constant. This requires threading diet_style into calculate_macros (currently absent) so goal and diet_style jointly drive macros. Note: with the carb cap in place the "validate the pool can supply the carb target" step becomes a backstop rather than the primary fix — the 31-food keto pool (mean 4.1g carb/100g) easily covers a <=50g carb target, so capping carbs resolves the supply mismatch directly. Separately, wiring validate_day into NutritionService.generate (it is imported but never called) is a worthwhile guardrail to stop any off-target day shipping silently.
- 🟠 **HIGH** `app/domain/nutrition/meal_builder.py:284-345` — **5- and 6-meal plans collapse to unbalanced protein-only snacks (slot-name mismatch)**
  - _MEAL_NAMES for 5/6 meals include 'mid_morning','afternoon_snack','evening_snack' (lines 287-288), but every FoodItem.meal_slots in food_db.py uses only breakfast/lunch/dinner/snack. _pick filters `slot_name in f.meal_slots` (line 330), so for these slots it returns nothing; the `len(selected)<2` fallback (line 340) ALSO filters by `slot_name in f.meal_slots` so still returns nothing; only the final `selected = available[:3]` (line 344) fires, yielding 3 arbitrary same-category foods. Verified: a 5-meal plan's 'mid_morning' slot became ['chicken_breast','chicken_thigh','salmon'] (3 proteins, no carb/veg/fat). The client's mid-morning/afternoon/evening meals are unbalanced protein dumps.
  - **Fix:** Either map the extra slot names to real food meal_slots (treat mid_morning/afternoon_snack/evening_snack as 'snack', breakfast, or main), or add those names to the relevant foods' meal_slots. Make _pick fall back to slot-agnostic selection that still balances categories.
  - _Verifier note:_ Suggested fix is correct. Cleanest concrete implementation: add a slot-alias map normalizing mid_morning/afternoon_snack/evening_snack -> 'snack' (semantically these are between-meal eatings) and apply it to the `slot_name in f.meal_slots` test in both _pick (line 330) and the fallback (line 340). Additionally make the fallback slot-agnostic but still pick one-per-category (protein/grain/veg/fat) so the final `selected = available[:3]` same-category dump (line 344) never produces an all-protein meal. Either alias-mapping OR adding the extended names to foods' meal_slots resolves it; aliasing is lower-touch since food_db has ~150 entries.

### Exercise database quality
- ⚪ **LOW** `app/exercise_db.py:42,3` — **True clone: two identical Smith Machine Standing Calf Raise entries**
  - smith_standing_calf_raise and smith_calf_raise identical (calves, smith_machine, mid_range, fatigue 2) except id.
  - **Fix:** Remove smith_calf_raise.
  - _Verifier note:_ Fix is locally correct: remove the entry whose exercise_id == "smith_calf_raise" (keep smith_standing_calf_raise). Note two corrections for the parent: (1) the cited location "app/exercise_db.py:42,3" is wrong — exercise_db.py is a single-line file, so all 179 entries are on line 3; identify the entry by exercise_id, not line number. (2) This is NOT the only true clone in the catalog — the same data-hygiene defect exists for at least two more pairs: "Single-Leg Leg Extension" (leg_extension_machine_single_leg vs machine_leg_extension_single_leg — identical) and "Incline Dumbbell Fly" (db_fly_incline vs db_incline_fly — identical). Two cable-fly name-pairs (cable_fly_low_to_high/cable_low_to_high_fly and cable_fly_high_to_low/cable_high_to_low_fly) are near-clones differing only in fatigue_cost (2 vs 1). A complete fix should de-dup the catalog by (name + attributes), not just delete this one row.
- 🟠 **HIGH** `app/exercise_db.py:3,55` — **True clone: two identical Single-Leg Leg Extension entries**
  - leg_extension_machine_single_leg and machine_leg_extension_single_leg identical (quadriceps, leg_extension_machine, shortened, fatigue 2) except id.
  - **Fix:** Remove machine_leg_extension_single_leg.
  - _Verifier note:_ Removing machine_leg_extension_single_leg (the line-55 appended duplicate) is correct and safe — first verify no override map, test, or coach config references that exact id (grep shows only the single definition; no other references found). Stronger remediation: this is one of 5 duplicate-name clusters in the DB, so either (a) de-duplicate all five name clones, and/or (b) make generator dedup robust by tracking a normalized (name) key in used_ids in addition to exercise_id, so future id-distinct clones cannot both be selected in one plan.
- 🟡 **MEDIUM** `app/exercise_db.py:3,71` — **Duplicate High-to-Low Cable Fly with contradictory fatigue_cost 2 vs 1**
  - cable_fly_high_to_low fatigue 2 and cable_high_to_low_fly fatigue 1 share name, chest, cable_machine, shortened. Fatigue feeds slot eligibility (generator.py:276) and both can appear via dedup-by-id.
  - **Fix:** Delete cable_high_to_low_fly and pick one fatigue value.
  - _Verifier note:_ Deleting cable_high_to_low_fly (the unreachable fat-1 duplicate) and keeping cable_fly_high_to_low (fat 2, the variant actually selected) is correct. But scope it wider: this is 1 of 5 duplicate-named pairs. Apply the same de-dup to: Low-to-High Cable Fly (cable_fly_low_to_high fat 2 vs cable_low_to_high_fly fat 1 — same contradictory split, delete the fat-1 cable_low_to_high_fly), Incline Dumbbell Fly (db_fly_incline vs db_incline_fly), Single-Leg Leg Extension (leg_extension_machine_single_leg vs machine_leg_extension_single_leg), Smith Machine Standing Calf Raise (smith_standing_calf_raise vs smith_calf_raise) — these three share fatigue so they are pure dupes (delete either). Additionally, to prevent recurrence, add a startup/test assertion that exercise names (not just ids) are unique, since names are rendered to users and there is no name-based dedup in the pipeline.
- 🟡 **MEDIUM** `app/exercise_db.py:3,72` — **Duplicate Low-to-High Cable Fly with contradictory fatigue_cost 2 vs 1**
  - cable_fly_low_to_high fatigue 2 and cable_low_to_high_fly fatigue 1 share name, upper_chest, cable_machine, shortened; only fatigue and id differ.
  - **Fix:** Delete cable_low_to_high_fly and reconcile fatigue.
  - _Verifier note:_ Suggested fix (delete cable_low_to_high_fly, keep cable_fly_low_to_high, reconcile fatigue) is correct. Pick fatigue_cost=2 (the more anatomically reasonable value for a cable upper-chest fly) and keep the richer secondary_muscles ['chest','front_delts']. NOTE: this is not isolated — the same audit query found 5 duplicate-name pairs in EXPANDED_EXERCISES_DATA: 'Low-to-High Cable Fly', 'High-to-Low Cable Fly', 'Incline Dumbbell Fly', 'Single-Leg Leg Extension', 'Smith Machine Standing Calf Raise' (179 total entries, all exercise_ids unique). The fix should be generalized to dedup all 5 pairs and add a startup/test assertion that names are unique (not just ids), so the same physical movement can never be served twice in a week with inconsistent fatigue costs.
- ⚪ **LOW** `app/domain/nutrition/food_db.py:18` — **CROSS-SUBSYSTEM string-slice obfuscation yields junk diet tag egan on Chicken Breast**
  - chicken_breast diet tag is built by slicing string literals (vegan sliced [1:] plus egan sliced [1:][:0]) which evaluates to egan, a junk tag. Chicken gets no real diet flag yet the slice resembles vegan. Only line 17-18 uses it.
  - **Fix:** Use the literal list omnivore/keto/gluten_free; never a vegan tag for chicken.
  - _Verifier note:_ The suggested fix (replace the slice with the literal list ["omnivore","keto","gluten_free"]) is correct and is the right cleanup. Caveat: the finding overstates impact. The expression evaluates to 'egan', NOT 'vegan', so chicken does NOT and cannot leak into vegan plans (verified: vegan filter excludes it, keto filter includes it). This is inert junk/obfuscation, a data-hygiene issue, not a functional filtering bug. Drop "egan" entirely and keep the three real tags; no vegan tag was ever actually applied to chicken.

### Telegram bot flows
- 🟠 **HIGH** `app/bot.py:3509-3597` — **handle_nutrition_approve / handle_nutrition_discard have no authorization or scope check**
  - Both handlers are registered as standalone global CallbackQueryHandlers (lines 5140-5141, patterns ^nutrapprove: and ^nutrdiscard:) and neither performs any role or scope check. handle_nutrition_approve does `plan_id = int(query.data.split(':')[1])`, then activates the NutritionPlan, supersedes prior active plans, renders a PDF and DMs it to the client — with no is_super_admin / is_coach / _user_can_act_on_client gate. This is in direct contrast to the workout sibling handle_admin_approve (line 3754) and handle_open_pending_item (4289), which both call `_user_can_act_on_client(update.effective_user.id, pending.client_id)` before acting. plan_id is a sequential integer (NutritionPlan.id autoincrement), so among the bot's own coaches there is no horizontal-privilege isolation: coach A can approve/discard coach B's client's nutrition draft by acting on any plan id. handle_nutrition_discard likewise sets status='rejected' on any plan id with no check.
  - **Fix:** Add the same gate the workout path uses at the top of both handlers: load the NutritionPlan, then `if not _user_can_act_on_client(update.effective_user.id, plan.client_id): await query.edit_message_text('🔒 Not authorized for this client.'); return`. Reject the action and log coach_scope_violation, mirroring handle_admin_approve.
  - _Verifier note:_ Fix is correct and directly implementable. handle_nutrition_approve already loads `plan` at line 3516 and NutritionPlan has a client_id field (models.py:202), so insert the gate immediately after the `if not plan` check (after 3519) and before the supersede/activate writes: `if not _user_can_act_on_client(update.effective_user.id, plan.client_id): logging.warning('coach_scope_violation handler=nutr_approve user_id=%s client_id=%s plan_id=%s', update.effective_user.id, plan.client_id, plan_id); await query.edit_message_text('🔒 Not authorized for this client.'); return`. Apply the identical gate in handle_nutrition_discard after loading the plan (after line 3591) — note the existing discard code only acts `if plan:`, so the gate must run inside that branch (or load+null-check first) and must precede the status='rejected' write. This mirrors handle_admin_approve/handle_open_pending_item exactly, including the coach_scope_violation log line.
- 🟠 **HIGH** `app/bot.py:4787-4809` — **handle_safety_clear bypasses a medical safety gate with no authorization check**
  - handle_safety_clear is a global CallbackQueryHandler (line 5145, pattern ^safety_clear:) that, on callback `safety_clear:<client_id>:<condition_key>`, writes profile.safety_override_note = 'Cleared by physician for ...' — permanently disarming the SafetyRefusalError gate (raised in run_generation_and_dispatch line 559) for that condition so the next plan generates unchecked. The handler does NO role check whatsoever (no is_super_admin, no is_coach, no _user_can_act_on_client). The clearance button is only ever sent to _admin_chat_id() (line 562), so in normal operation only the admin can press it, but the handler relies entirely on button distribution for security: there is zero runtime gate on a medical-safety bypass. Given that overriding a physician-clearance gate is the single most safety-critical action in the bot, it must not depend on UI distribution alone.
  - **Fix:** Gate the handler: `if not (auth_roles.is_super_admin(update.effective_user.id) or _user_can_act_on_client(update.effective_user.id, client_id)): await query.edit_message_text('🔒 Not authorized.'); return`. Restrict to super-admin or the client's assigned coach, and log who cleared the gate.
  - _Verifier note:_ The suggested fix is correct and idiomatic: _user_can_act_on_client(user_id, client_id) already exists at app/bot.py:108 and is the exact pattern handle_admin_approve uses, and auth_roles.is_super_admin lives in app/auth/roles.py (imported as auth_roles at bot.py:41). Insert the gate right after parsing client_id (after line 4793), using update.effective_user.id and the parsed client_id, then edit_message_text a "Not authorized" notice and return. Two refinements: (1) For a medical-liability gate, prefer restricting to super-admin only (auth_roles.is_super_admin) rather than any assigned coach; _user_can_act_on_client would also let the client's assigned coach clear the gate, which matches the approve convention but is looser than ideal for a physician-clearance bypass — pick based on policy. (2) Log who cleared the gate (mirror the coach_scope_violation logging.warning in handle_admin_approve and add a logging.info on successful clearance recording user_id, client_id, condition_key) for audit trail, since this is a safety-critical action.

### FastAPI web API + auth
- 🔴 **CRITICAL** `app/settings.py:58` — **JWT signing secret defaults to a publicly-known placeholder**
  - auth_secret_key defaults to "change-me-in-production" (settings.py:58) and jwt.py:_secret_key() reads this value to sign/verify ALL tokens with HS256. main.py:152 only WARNS at startup if the default is in use — it does not refuse to boot. If a deployment ships with the default (or any leaked secret), an attacker computes jwt.encode({'sub':'cl_victim','exp':...}, 'change-me-in-production', algorithm='HS256') and get_current_user (deps.py:36-42) accepts it as that user — full account takeover for every client, coach, and admin (admin status is derived from the resolved ClientProfile row).
  - **Fix:** Refuse to start when auth_secret_key is empty or equals the placeholder (raise in lifespan instead of logger.warning), and require AUTH_SECRET_KEY to be set with no default in production. Fail closed, not open.
  - _Verifier note:_ The suggested fix (fail closed in lifespan instead of logger.warning; require AUTH_SECRET_KEY with no usable default) is correct and addresses the exploit. Two refinements: (1) The threat is not limited to the literal placeholder — any weak/leaked/short secret allows the same forgery. Enforcement should also reject secrets below a minimum length/entropy (e.g. < 32 bytes), not only the exact string "change-me-in-production". (2) The empty-string case is already grouped with the placeholder at main.py:152 (`in ("", "change-me-in-production")`); both must be converted from logger.warning to a hard raise (e.g. RuntimeError) so the app refuses to start. Consider gating the hard-fail on a non-dev environment if you want to keep SQLite-dev convenience, but the production path must fail closed. Optionally bind tokens to a per-user token version/secret salt so secret rotation invalidates outstanding forged tokens.
- 🟠 **HIGH** `app/auth/jwt.py:41-47` — **Access-token verifier accepts refresh/reset/verify tokens (token type confusion)**
  - decode_token() decodes any HS256 token and returns its `sub` without checking the `type` claim, unlike decode_refresh_token / decode_reset_token / decode_verify_token which all enforce `type`. get_current_user (deps.py:36) authenticates with decode_token. Empirically confirmed: create_refresh_token('cl_victim'), create_reset_token('cl_victim'), and create_verify_token('cl_victim') all decode successfully through decode_token and authenticate as that user. Reset and verify tokens are emailed to users (auth.py:88-92, 149-153) and therefore land in inboxes, mail-server logs, and support tickets — any leaked one becomes a full API access token for its TTL (30 min reset, 48 h verify, 30 day refresh). test_api.py:120 tests the reverse direction (refresh path rejects access token) but nothing tests this direction.
  - **Fix:** Give access tokens an explicit type claim (e.g. "access") in create_access_token and have decode_token reject any token whose `type` is not "access". Add a test asserting reset/verify/refresh tokens are rejected by get_current_user.
  - _Verifier note:_ Correct and standard. Add "type": "access" to create_access_token and have decode_token reject any token whose type != "access" (treat missing type as invalid). Two rollout caveats worth noting: (1) all access tokens already issued lack the claim, so a strict check invalidates them on deploy and forces re-login; if that is undesirable, accept a missing 'type' as 'access' for a short deprecation window before tightening. (2) Add a test asserting reset/verify/refresh tokens are rejected by get_current_user (test_api.py:120 only covers the already-enforced reverse direction). A more robust long-term variant is to give every token an explicit, mutually-exclusive type and validate it in each decoder.
- 🟠 **HIGH** `app/routes.py:28-55` — **/generate and /generate_and_coach are unauthenticated and accept arbitrary ClientProfile bodies**
  - Both POST routes take `client: ClientProfile` straight from the request body with no Depends(get_current_user) and no auth, and they are mounted via app.include_router(router) in main.py:243 (no auth prefix/dependency). /generate_and_coach (routes.py:35-55) writes a ProfileSnapshot row with the attacker-supplied client_id (line 44) AND calls the paid LLM (line 53). test_api.py:40 posts to /generate_and_coach with no auth header, confirming this is the live behaviour. This is an unauthenticated cost-amplification + DB-pollution vector (an attacker can spam LLM calls and insert ProfileSnapshot rows under any client_id they choose).
  - **Fix:** Either gate these routes behind get_current_user (and ignore the body's client_id, using the authenticated user), or remove them entirely now that /api/v1/plans/generate is the real path. At minimum require auth before the LLM call and snapshot write.
  - _Verifier note:_ Fix is correct and well-scoped. Preferred: since /api/v1/plans/generate is the real authenticated path, remove these two legacy root-mounted routes (and the corresponding test). If they must stay, add a router-level or per-route Depends(get_current_user) (or attach the dependency at include_router in main.py:243), and derive client_id from the authenticated principal rather than trusting client.client_id from the body — otherwise an authenticated user could still write a ProfileSnapshot under an arbitrary client_id. The unauthenticated /generate route should be gated too, not just /generate_and_coach, since it still runs generation on arbitrary input (lower impact: no LLM/DB write, but no reason to leave it open). One nuance: the snapshot write and LLM call happen even with auth, so the body's client_id must be ignored/overridden, not merely require a token.
- 🟠 **HIGH** `app/api/profile.py:93-125` — **Account deletion leaves recoverable PII (email/name) in ProfileSnapshot and Feedback**
  - delete_my_account anonymises only the ClientProfile row (clears name/email/password_hash) and deletes PendingApproval. It never touches ProfileSnapshot.snapshot_json — which is a full JSON-serialised ClientProfile including email and name (models.py:79, written by routes.py:44 and the generator) — nor Feedback.email, which feedback.py:33 copies from user.email into its own column. Both tables are still returned verbatim by GET /profile/export (profile.py:74,73,87). The docstring also claims it "scrambles client_id linkage" but the code never modifies client_id. So a GDPR deletion request leaves the user's email recoverable in at least two tables and exportable.
  - **Fix:** On delete, also delete or scrub ProfileSnapshot rows for the client_id and null/redact Feedback.email (and any other PII-bearing tables). Fix or remove the false "scrambles client_id linkage" docstring claim.
  - _Verifier note:_ Fix is correct and should be implemented. Refinements:

- Reproducible defect is data-AT-REST persistence after a response saying anonymised:True, not "still exportable." After delete, email/password_hash are nulled so the user can no longer authenticate to GET /export (self-scoped), so phrase the harm as "PII recoverable in the database / via any operator or admin query," not "exportable by the user."

- On delete, also DELETE (preferred for true erasure) or scrub ProfileSnapshot rows where client_id == cid (scrub means rewriting snapshot_json to strip email/name), and null Feedback.email for the client_id. Plain DELETE of both is simplest and matches GDPR erasure intent better than the current "keep history for analytics" justification.

- Enumerate ALL PII-bearing tables, not just the two named. AuditEvent.actor_email and AuditEvent.target (models.py:101,103) can hold the user's email and are also untouched by delete (and exported at profile.py:78/89). PendingApproval is deleted so it's covered, but PendingApproval.client_email exists (models.py:89) — confirm nothing else stages it. Audit the full set before claiming erasure.

- Fix or remove the false docstring (profile.py:98-99): it cannot scramble client_id (primary key + referenced by FKs at models.py:164,203), so drop that claim.

### PDF rendering + email dispatch
- 🟠 **HIGH** `app/adapters/pdf/templates/partials/_meal_card.html.j2:14` — **Meal card crashes on every real nutrition plan (dict.items footgun) — nutrition PDF never delivered**
  - Line 14: `{% for food, grams in slot.items %}`. `slot` is a plain dict (parsed from NutritionPlan.plan_json), so Jinja resolves `slot.items` to the bound dict.items METHOD (attribute lookup precedes subscript), not the 'items' list. Iterating a method object raises `TypeError: 'builtin_function_or_method' object is not iterable`. Verified by rendering this partial with the exact slot dict that nutrition_service.py (lines 150-172) emits — it crashes. Even if it reached the list, items are 8-key dicts (name/slug/category/grams/kcal/...) and would fail tuple-unpack into (food, grams) with `ValueError: too many values to unpack`. Because render_plan_pdf wraps the whole render, this raises, and the nutrition-approve handler (bot.py:3554) catches it, sets pdf_bytes=None, and sends the client a text-only fallback ('Ask your coach for details') — so EVERY nutrition client gets zero meal/macro content. The 230 tests pass because tests/test_pdf.py:_make_nutrition_plan() builds plan_json days with NO 'slots' key, so this loop never executes.
  - **Fix:** Use subscript and a single loop var matching the real shape: `{% for item in slot['items'] %}` then render `{{ item.name }}` and `{{ item.grams | round | int }} g`. Replace lines 16-17 accordingly (item.name, item.grams).
  - _Verifier note:_ Suggested fix is correct and I verified it renders. Replace line 14 with `{% for item in slot['items'] %}` and lines 16-17 with `{{ item.name }}` and `{{ item.grams | round | int }} g`. Using subscript `slot['items']` (or Jinja's `slot.get('items', [])` for null-safety against slots that omit items) avoids the dict.items method-shadowing footgun, and iterating with a single loop var `item` matches the real 8-key dict shape. Optionally also defend the parent meal_plan.html.j2:18 loop with `day_data.get('slots', [])` semantics, though that one already works because day dicts here do carry 'slots'. The empty-list null-safety is a minor hardening beyond the reported fix.
- 🟠 **HIGH** `app/adapters/pdf/templates/sections/shopping_list.html.j2:14` — **Shopping list crashes on real plan — reads item.total_g but renderer builds total_grams**
  - Line 14: `{{ item.total_g | round | int }} g`. The renderer's _build_nutrition_context (renderer.py lines 238-242) builds each shopping-list item with key `total_grams`, not `total_g`. So `item.total_g` is Jinja Undefined, and `Undefined | round` raises `TypeError: type Undefined doesn't define __round__ method`. Verified: rendering this section with the dict that _build_nutrition_context actually produces (keys name/slug/total_grams) crashes. Independent of the meal_card bug — even if meal_card were fixed, shopping_list still crashes. Tests miss it because the test fixture aggregates an empty shopping_list (no 'items' in fixture days), so this loop body never runs.
  - **Fix:** Change `item.total_g` to `item.total_grams` (line 14), OR rename the renderer key. Align the two; the renderer key total_grams is the source of truth.
  - _Verifier note:_ Correct: change `item.total_g` to `item.total_grams` on shopping_list.html.j2:14 — the renderer key total_grams is the source of truth (set/accumulated in renderer.py at lines 236 and 241), so align the template to it. Optional hardening: also consider `{{ item.total_grams | default(0) | round | int }}` to be Undefined-tolerant, though the rename alone fully fixes the live crash. Note the renderer writes `total_grams` (not `grams`); the per-day item key `grams` is distinct and lives only in the nested plan_json, so no further change needed.
- 🟠 **HIGH** `tests/test_pdf.py:100-119` — **PDF tests use an unrealistic nutrition fixture that hides both nutrition-render crashes**
  - _make_nutrition_plan() (lines 100-119) builds plan_json as a list of day dicts containing ONLY top-level macros (day/kcal/protein_g/fat_g/carb_g/fiber_g) with NO 'slots' key and therefore no per-slot 'items'. Real nutrition_service.generate (nutrition_service.py:142-176) emits days WITH 'slots', each slot WITH an 'items' list. Consequently: meal_plan.html.j2's `{% for slot in day_data.slots %}` iterates an Undefined (empty) loop so the meal_card crash never fires, and _build_nutrition_context aggregates an empty shopping_list so the shopping_list crash never fires. This is exactly why 230 tests pass while the live nutrition PDF is 100% broken — the templates are never exercised against production-shaped data.
  - **Fix:** Replace the fixture's plan_json with the real nutrition_service shape (days with 'slots', each slot with an 'items' list of name/slug/category/grams dicts) and add a test that renders sections/meal_plan.html.j2 + sections/shopping_list.html.j2 end-to-end, asserting food rows and gram quantities appear. This would have caught both crashes.
  - _Verifier note:_ The suggested fix is correct IN SCOPE (this is a test-gap finding; replacing the fixture with the real nutrition_service shape and asserting food rows + gram quantities WOULD catch both crashes). But the test fix alone leaves nutrition PDFs broken in production. The actual code repairs needed: (1) partials/_meal_card.html.j2:14 — change `{% for food, grams in slot.items %}` to `{% for item in slot['items'] %}` and reference item.name / item.grams (the items are JSON dicts, not (food,grams) tuples, and slot.items collides with the dict .items() method). (2) sections/shopping_list.html.j2:14 — change `item.total_g` to `item.total_grams` to match the key built in renderer.py:240. Note the meal_card crash currently shadows the shopping_list crash in the live full render (meal_plan included before shopping_list in plan.html.j2), so fixing only #1 will then expose #2 — both must be fixed together. Optionally tighten the swallowing try/except at bot.py:3554 so a broken nutrition PDF is surfaced rather than silently dropped.

### LLM, checkin, audit services
- 🟠 **HIGH** `app/bot.py:2736-2790` — **Check-in extraction failure silently advances the week and discards the client's telemetry with no error shown**
  - In _finalize_checkin, when extract_checkin raises (e.g. transient OpenRouter outage), the except at line 2739-2741 sets `extraction = None` and swallows the error (only logs a warning). Execution falls through: the telemetry write-back is skipped because line 2767 guards on `if extraction and extraction.exercises:`, autoregulation is skipped (line 2797 `if extraction is not None:`), but line 2788 still runs `client.week_number += 1` and commits, and run_generation_and_dispatch is called at 2863 to produce a fresh plan. The user is never told their check-in failed. Net effect: the client's reported loads/RPE for the week are lost, the week counter advances so they cannot re-check-in for that week, and the next plan is generated as a normal progression with zero feedback incorporated.
  - **Fix:** If `extraction is None`, send the user an explicit failure message and `return` WITHOUT incrementing week_number or dispatching a new plan, so they can retry /checkin. Only advance the week once extraction (or at least the telemetry write) succeeded.
  - _Verifier note:_ Direction is correct: when extraction is None, send the client an explicit failure message ("Couldn't process your check-in — please run /checkin again to retry"), and `return ConversationHandler.END` BEFORE the week_number increment (2788) and before run_generation_and_dispatch (2863). The week must only advance once telemetry was actually written or extraction succeeded. Two additions: (1) the structured-button input path (bot.py:2700-2715) is equally exposed — button-entered weights/RPE are funneled into raw_text and round-tripped through the same LLM extraction, so they are also lost on extraction failure; the robust fix writes telemetry back directly from context.user_data['checkin_structured_results'] rather than re-extracting user-confirmed numbers via the LLM. (2) Even the early-return alone is the minimum correct fix for what was reported; the structured-path hardening is the durable follow-up.
- 🟠 **HIGH** `app/bot.py:2431-2436` — **Bot passes exercise NAMES as the lift catalog, but the extraction prompt/schema require canonical exercise_ids**
  - The bot builds `checkin_lift_catalog` from `slot.exercise_name` (display strings like 'Barbell Back Squat') at lines 2431-2436, and passes it to extract_checkin (bot.py:2738). But prompts/checkin_extract.j2 line 23-24 presents this list as '## Available exercise catalog (canonical IDs)' and CheckInExtraction.exercise_canonical must be a catalog exercise_id (schema.py:30). The autoregulation match in derive_plan_delta/_find_slot keys on `slot.exercise_id` (autoregulation.py:118,183). So the LLM is shown names, told they are ids, and must emit ids that match exercise_id — a type mismatch that makes exercise_canonical almost never line up with a real slot, so per-exercise RPE-error autoregulation (Rule 4) and telemetry write-back (bot.py:2774) rarely fire. CheckInService.ingest does this correctly with `ex['exercise_id']` (checkin_service.py:32), but that service is never called.
  - **Fix:** Build the catalog from exercise_id (as CheckInService.ingest does), or pass a name→id map and resolve names to ids after extraction. Align the bot path with the service.
  - _Verifier note:_ Suggested fix (build catalog from exercise_id, as CheckInService.ingest does) is correct and minimal — it gives the LLM the literal ID strings to copy back, which is the entire point of a canonical catalog, so the equality match at bot.py:2774 and _find_slot at autoregulation.py:183 can line up. A strictly more robust fix is available: the structured check-in path already knows exercise_id per slot (stored in checkin_structured_results keyed on exercise_id at bot.py:2605/2630), so telemetry can be written to slots directly from that dict, bypassing the LLM name->id round-trip entirely instead of re-extracting display-name text (bot.py:2712). Use exercise_id catalog as the minimal fix; direct-write from structured results as the robust one. Also note the prompt header/examples already expect IDs, so the prompt itself needs no change once the catalog carries real IDs.
- 🟠 **HIGH** `app/api/coach.py:204-221` — **FastAPI coach-edit writes LLM output to the active plan with no WorkoutWeek schema validation**
  - edit_plan calls apply_coach_edits (line 204), which only validates JSON *syntax* (llm_service.py:124 `json.loads(content)`), not the WorkoutWeek structure. The result is written straight to pending.workout_json at line 217 with no WorkoutWeek.model_validate_json. approve_plan then does `workout = json.loads(pending.workout_json)` (coach.py:153) and persists it to WorkoutHistory (line 162) — again no schema validation. So a schema-valid-JSON-but-wrong-shape LLM edit (e.g. `{}`, missing `days`, dropped slots) silently corrupts the client's active plan; the corruption only surfaces later when something tries to load it as a WorkoutWeek. The bot path (bot.py:4018) at least calls WorkoutWeek.model_validate_json before saving; the FastAPI path does not.
  - **Fix:** After apply_coach_edits, run WorkoutWeek.model_validate_json on the result before assigning to pending.workout_json; on ValidationError return 422 and leave the prior plan intact.
  - _Verifier note:_ Correct AND sufficient. After apply_coach_edits in edit_pending_plan (coach.py:204), run WorkoutWeek.model_validate_json(edited_json); on pydantic ValidationError return HTTP 422 and leave pending.workout_json untouched (do not commit). This alone closes the hole completely — coach.py:217 is the ONLY unvalidated writer to pending.workout_json. Every other writer already produces a validated WorkoutWeek: plans.py:100/120 call week.model_dump_json() on a real WorkoutWeek object, and bot.py:4026 dumps an already-validated object. So once the edit path validates, approve_plan's raw json.loads at coach.py:153/162 is safe by construction — you do NOT also need to add validation in approve_plan. Optional defense-in-depth: re-validate in approve_plan too, but it is not required to fix this finding.

### Flutter mobile app
- 🟠 **HIGH** `mobile/lib/features/coach/coach_review_screen.dart:349` — **Coach review exercise names all '?'**
  - Reads slot['exercise']['name'] but slots are flat exercise_name.
  - **Fix:** Use slot['exercise_name'].
  - _Verifier note:_ Correct. Replace lines 349-350 with a single flat read: `final name = slot['exercise_name'] as String? ?? '?';` and delete the `final exercise = slot['exercise'] ...` line (it is now unused). This matches the shape used everywhere else in the app (checkin_screen.dart, workout_screen.dart, plan_screen.dart) and the backend WorkoutSlot.exercise_name field. The other reads on the same card (slot['sets'], slot['reps'], slot['target_weight'], slot['rpe'] at lines 351-354) are already correct and need no change.

### Test suite gaps
- 🔴 **CRITICAL** `app/domain/nutrition/meal_builder.py:109-114` — **Medical-condition filtering untested; produces EMPTY pool for diabetic/hypertensive clients**
  - filter_food_pool requires low_sugar or low_sodium in f.medical_tags but all 68 foods in food_db have empty medical_tags. Verified live: filter_food_pool with medical_conditions type2_diabetes returns 0 foods and hypertension returns 0. nutrition_service.py:93 passes profile.medical_conditions straight through, so a diabetic or hypertensive client's plan is built from an empty pool and build_day_plan falls back to an empty selection yielding total kcal 0. No test exercises this path.
  - **Fix:** Add tests asserting filter_food_pool with type2_diabetes and hypertension returns a non-empty pool and that build_day_plan over it yields kcal above 0 passing validate_day; populate food_db medical_tags or relax the filter to read sodium and sugar columns.
  - _Verifier note:_ The suggested fix is correct but should be expanded. Minimum to stop the data-corruption: (a) populate food_db medical_tags (tag low-sodium and low-sugar/low-carb items) OR relax the filter to read nutrient columns directly (e.g. hypertension: keep low-sodium category foods; type2_diabetes: f.carb_per_100g < 25 alone without requiring the absent low_sugar tag). Either makes the pool non-empty. (b) Independently, add a hard guard in NutritionService.generate(): if filter_food_pool returns an empty (or too-small) pool, fail loudly / fall back to the unfiltered medical pool rather than committing a kcal-0 plan — and actually CALL the already-imported validate_day before persisting/activating, since right now no validation gate exists at all (validate_day is imported but never invoked). (c) Also handle unrecognized free-text medical strings from bot.py:3435 (e.g. "high_cholesterol", "ibs" from the prompt's own examples are silently ignored — only the two literals are handled), so plans aren't silently committed for conditions the engine doesn't actually adjust for. Add the regression tests the finding describes for both conditions plus the empty-pool guard.
- 🔴 **CRITICAL** `app/domain/nutrition/meal_builder.py:102-103` — **Religious-restriction filtering untested; halal/kosher filter is dead and leaks pork and shellfish**
  - filter_food_pool removes foods whose religious_restrictions intersect the client's, but all 68 foods have empty religious_restrictions, so it never removes anything. Verified live: filter_food_pool with religious_restrictions halal returns all 68 foods including pork_tenderloin, lean_beef, beef_sirloin and shrimp. nutrition_service.py:90 wires profile.religious_restrictions in, so a halal or kosher client is served forbidden foods. No test asserts religious filtering removes anything.
  - **Fix:** Add a test asserting filter_food_pool with halal excludes pork_tenderloin and that at least one food carries a religious tag; it fails today, forcing food_db to tag non-halal and non-kosher items.
  - _Verifier note:_ Directionally correct but incomplete as written — a test alone fixes nothing; the real remedy is data tagging in food_db.py, and the tagging semantic is load-bearing and easy to invert. Because the filter is `r in f.religious_restrictions` (food removed when the client's restriction system tags it), each food must be tagged with the restriction system that FORBIDS it: pork_tenderloin -> ["halal","kosher"]; shrimp and any other shellfish -> ["kosher"]. Do NOT tag pork as "halal" meaning "is-halal" (that inverts the semantic and makes it worse), and do NOT invent a freeform "pork" tag the filter never checks. Then add tests asserting filter_food_pool(..., religious_restrictions=["halal"]) excludes pork_tenderloin and religious_restrictions=["kosher"] excludes pork_tenderloin AND shrimp. One precision correction to the finding's framing: shellfish (shrimp) is primarily a KOSHER prohibition; mainstream halal generally permits seafood, so don't tag shrimp halal-forbidden. The pork->halal leak is the unambiguous halal case. (Separate, deeper gap not folded in here: there is no zabiha/halal-slaughter concept, so strictly all meat is non-halal — out of scope for this fix.)
- 🟠 **HIGH** `tests/test_generator.py:15-188` — **Generator never swept across all avatar by days combos; untested combos emit EMPTY training days**
  - Tests cover only powerbuilder-4, powerlifter-4, gen_pop-3 and gen_pop-5. Sweeping all 21 cells through generate reveals: powerlifter-6 (a push pull legs split) produces an EMPTY Pull day with 0 slots because the powerlifter exercise pool cannot fill a pull day; and all three avatars at days 7 produce an EMPTY Training Day 7 from the else branch of _resolve_split at generator.py lines 97 to 98. A client picking 6 days as a powerlifter, or 7 days for any avatar, receives a blank workout day.
  - **Fix:** Add a parametrized test over powerlifter, powerbuilder and gen_pop times days 1 through 7 asserting every generated day has at least one slot, or that unsupported day counts raise a clear error; it fails on powerlifter-6 and all avatars at 7.
  - _Verifier note:_ The proposed parametrized sweep asserting every generated day has >=1 slot is correct and would fail on powerlifter-6. Two refinements: (1) Scope the assertion to the bot-reachable range days 3-6 (not 1-7); days=7 is only reachable via the testing/integration API, so anchor the test and the severity on powerlifter-6. (2) The underlying production fix is NOT in the exercise DB (the pool is adequate — selection succeeds). It is the per-week volume budget being shared globally across all 6 days so the duplicated Pull day in the 6-day PPL split runs into a fully-spent "back" budget and zeroes out every slot (generator.py:416-418). Real fixes: either budget per-session/per-day rather than per-week, raise budgets when training_days >= 5 (so 2x Pull days fit), or add an empty-day fallback in _fill_slots that backfills at least one slot ignoring budget when slots == 0. Also worth adding: a guard in run_generation_and_dispatch (or _fill_slots) that refuses to dispatch a week containing any 0-slot day, and enforcing le=6 on the /generate request body so the API can't emit Training Day 7.
- 🟠 **HIGH** `app/services/nutrition_service.py:85-115` — **Nutrition orchestrator (BMR to TDEE to macros to 7 day meal build to validate) has zero tests**
  - Lines 85 to 115 wire the entire nutrition pipeline end to end (energy plus macros plus filter_food_pool plus seven build_day_plan calls plus persistence) and are the production entry point. No test references nutrition_service. test_nutrition.py only unit-tests energy.py and macros.py in isolation; the meal building half and the integration never run, so the empty-pool failures, the never-called validate_day, and any 0 kcal output reach production untested. An end to end probe over the food db for a healthy omnivore does validate (kcal 2403 versus target 2400, validate_day returns no errors), proving an integration test is feasible and cheap.
  - **Fix:** Add an integration test calling the public nutrition generation function for a healthy omnivore (7 days, each passing validate_day), a vegan, and a diabetic (assert non-empty and kcal above 0, currently fails), using an in-memory SQLite session like conftest.
  - _Verifier note:_ The suggested integration test (call NutritionService(session).generate() for omnivore/vegan/diabetic via in-memory SQLite like conftest) is correct and necessary — the omnivore case passes, the diabetic case fails with a 0-kcal plan, so the test would catch the regression. But the test only EXPOSES the bug; it does not remediate it. The test must drive three production fixes: (a) ROOT CAUSE — app/domain/nutrition/food_db.py has zero medical_tags on any of its 68 foods, so the type2_diabetes ("low_sugar") and hypertension ("low_sodium") branches in filter_food_pool (meal_builder.py:109-114) can never yield a non-empty pool; either tag the foods or relax/redesign those filters; (b) add an empty-pool guard in build_day_plan (meal_builder.py:292) or in NutritionService.generate() so an empty filtered_pool aborts with an explicit error instead of silently producing a 0-kcal day; (c) actually CALL validate_day in NutritionService.generate() after building each day (it is imported at line 21 but never invoked) and refuse to persist/deliver a plan that fails validation. Also worth covering hypertension (same 0-kcal failure, broader than the finding's diabetic-only scope) and asserting validate_day passes per day for the healthy case.

---

## ❌ Refuted by verification (do NOT fix — false alarms)

- `app/exercise_db.py:3,73` — True clone: two identical Incline Dumbbell Fly entries — _Both entries do exist in the live data — app/exercise_db.py has db_fly_incline and db_incline_fly, both name="Incline Dumbbell Fly", movement_pattern="isolation", primary_muscle="upper_chest", fatigue_cost=2, equipment ["dumbbells","bench"]_
- `app/database.py:9-20` — create_all defeats alembic — _The finding quotes real code (app/database.py:20 runs SQLModel.metadata.create_all(engine) unconditionally at import; create_db_and_tables() at line 25 wraps the same call and is invoked by the bot at app/bot.py:4873). But its stated mechan_
- `app/settings.py:33` — Divergent LLM model-id defaults; the fallback id (gemini-2.5-flash) is absent from OpenRouter's current catalog — _The finding's headline and severity rest on one claim: that the bot's fallback model id `google/gemini-2.5-flash` is "absent from OpenRouter's current catalog" and would 404/400, "taking down coach-message generation, coach-edits, FAQ, exer_

---

## Full findings by subsystem

### Workout generation engine (`workout-engine`)

_Workout generator audit. Sound on the happy path (beginner gen_pop 3-day clean; 5-week RPE block plus week-5 deload correct; intra-day de-dup works) but degrades silently in common paying-user cases without errors, so all 230 tests pass. Headline bugs: powerlifter 5/6-day splits ignore the avatar and collapse to thin days; the weekly budget is spent greedily so repeated day-types are starved; the live load-progression path is uncapped (+20pct) while the check-in delta caps at +/-10pct; slot_type is index-based so a day can lack a main_compound. /generate is unvalidated; the bot hardcodes full_gym; only lower_back_pain limitation works; budget keys aggregate muscles; core is never programmed._

**Coverage:** PRESENT: split routing 2-6 days; per-experience volume budget; correct 5-week RPE block plus deload; template slot filling with 3-tier fallback; warmup builder; capped derive_plan_delta plus uncapped AutoRegulator; log-only validation; safety gates; coach overrides; lower_back_pain. 179 exercises, 8 patterns. MISSING/THIN: tiny powerlifter pool (30) and 5/6-day powerlifter splits ignore avatar; only lower_back_pain limitation (SUBSTITUTION_MAP never imported); no bot equipment selection (hardcoded full_gym); all-equipment gate denies dumbbells-only a chest press; no minimum-slot guarantee; aggregated budget keys starve delt heads/arms; no per-week jump cap; core/abs never programmed.

- 🟠 **high** [correctness] ✅verified→high `app/generator.py:93-96` — **Powerlifter 5/6-day splits ignore avatar, collapse to thin days**: resolve_split returns a generic split (5d Upper/Lower/Push/Pull/Legs, 6d PPL/PPL) regardless of avatar; only 30 of 179 exercises are powerlifter-tagged, so advanced powerlifter 5-day gives Lower 2 slots, Legs 2 slots, and Upper/Push/Pull each end with Barbell Shrug as the only surviving isolation. → _Fix:_ Add powerlifter-specific 5/6-day templates and expand powerlifter avatar_tags, or allow powerbuilder accessories for powerlifters.
- 🟠 **high** [bug] ✅verified→high `app/generator.py:416-419` — **Weekly volume budget spent greedily in day order, starving repeated day-types**: budget is built once per week (line 471) and _spend_budget mutates it across all days in order, so when a day-type repeats (6-day PPL, 3-day FBA-twice) later occurrences get leftovers. Traced 6-day beginner: Push#1 5 slots/16 sets but Push#2 4 slots/11 sets; upper_chest accessory cut 3 to 1 set and side_delts isolation dropped (line 417). The two Push days are also identical exercises. → _Fix:_ Divide each muscle weekly cap by the number of days training it before filling, or trim evenly in a post-pass.
- 🟠 **high** [correctness] ✅verified→high `app/generator.py:30-46` — **AutoRegulator.calculate_next_load is uncapped and live, up to +20pct weekly jumps**: 4pct/RPE error correction plus 2.5pct/RPE stimulus with no clamp; RPE error -4 returns last_weight*1.20 (verified 100 to 120kg). Runs in _fill_slots (line 364) when prior_week exists and bot.py:465 calls generate(profile, prior_workout) on check-in, so the uncapped path is production-live. The parallel derive_plan_delta (autoregulation.py:127,134) caps the same correction at +/-10pct; inconsistent caps. → _Fix:_ Clamp calculate_next_load to 0.90-1.10x last_weight to match derive_plan_delta, and consolidate the two engines.
- 🟡 **medium** [bug] `app/generator.py:406-426` — **slot_type assigned by enumerate index; skipped specs let a day have no main_compound**: Line 426 sets slot_type by index (i0 main_compound, i1 secondary_compound, else isolation). Skipped specs (lines 408/410) still advance i. Dumbbells-only Upper: chest and row specs are skipped, the first appended slot lands at slot_order 3 labeled isolation, and the day has NO main_compound. Check-in (bot.py:383,485) logs telemetry only for main/secondary_compound, so these collect zero progression telemetry. → _Fix:_ Derive slot_type from the matched spec type (already passed at line 424) instead of overwriting by positional index at line 426.
- 🟡 **medium** [bug] `app/routes.py:28-39` — **FastAPI /generate accepts an unvalidated ClientProfile; bad inputs silently mis-generate**: SQLModel table=True does not enforce ge=3/le=6 or enums. ClientProfile(training_days=7, avatar=martian, experience_level=banana) constructs fine: days=7 falls back to 7 identical Full_Body_A days (line 332), experience=banana is treated as advanced (line 107), avatar=martian yields an empty plan; none error. → _Fix:_ Add a request schema with Literal avatar/experience and bounded training_days, or validate in generate.
- 🟡 **medium** [usability] `app/generator.py:136-181` — **Zero-candidate filters silently produce empty/thin days with no error or fallback**: No minimum-slot guarantee. gen_pop 3-day with empty available_equipment yields all 3 days empty (0-slot week); avatar=martian yields all empty (line 139); bodyweight-only 3-day yields 4 slots across the whole week. The engine reports success on a blank plan; push/pull (line 530) and volume validation only log warnings. → _Fix:_ After _fill_slots, raise a generation error when a day has under ~3 slots or the week is empty so the HITL coach is alerted.
- 🟡 **medium** [content-gap] `app/bot.py:2047` — **Bot hardcodes full_gym and the all-equipment gate denies dumbbells-only a chest press**: Intake (2047) and update (2032) force available_equipment=full_gym; the bot never asks about equipment, so home/dumbbell/bodyweight clients are unsupported and the _filter_exercises gate runs only via the unvalidated route or tests. The gate (generator.py:142-144) requires ALL listed equipment, so a dumbbells-only client is denied Dumbbell Bench Press (needs dumbbells+bench) and gets no horizontal chest press. → _Fix:_ Add an equipment-selection step to intake and the profile editor; treat bench-type accessories as optional or add bench-free dumbbell press variants.
- 🟡 **medium** [content-gap] `app/generator.py:150-156` — **Only lower_back_pain limitation works; SUBSTITUTION_MAP is never used**: _filter_exercises handles only lower_back_pain (line 152) and only by deleting hinge/lower-back exercises, never substituting. SUBSTITUTION_MAP (constants.py:67, shoulder_impingement and knee_pain) is never imported by generator.py, so those bot-collected limitations silently do nothing. → _Fix:_ Wire SUBSTITUTION_MAP into the exercise-selection fallback and handle the additional limitation keys the bot collects.
- 🟡 **medium** [correctness] `app/generator.py:48-63` — **Budget keys aggregate multiple muscles, starving later-drawn heads**: biceps+triceps map to arms and front/side/rear_delts to shoulders. _spend_budget draws the single shared cap per key in slot order; front-delt compounds run before side/rear-delt isolations, so those isolations get starved or dropped. Verified 5-day intermediate: arms 12 total and shoulders 12 total against a per-key budget of 16. VOLUME_LANDMARKS use the same aggregated keys so per-head starvation is not flagged. → _Fix:_ Budget per individual muscle, or order filling so isolations reserve a minimum share before compounds spend it.
- ⚪ **low** [content-gap] `app/config/workout_constants.toml:42-178, 18` — **No core/abs in any template (13 core exercises unused); MAX_FATIGUE 20 never binds**: No day_templates slot targets muscle core and core is not in MUSCLE_TO_BUDGET_KEY, so 13 core exercises are never programmed. Separately session.max_fatigue=20 but templates peak ~16, so the per-day cap (generator.py:410) never trims; CLAUDE.md value 12 is stale. Also generator.py:367 uses truthiness on actual_rpe (drops logged 0.0) and the prior-week scan reads only the first matching day. → _Fix:_ Add a core slot and budget key; lower/remove MAX_FATIGUE and update CLAUDE.md; use is-not-None for actual_rpe and aggregate across days.

### Nutrition domain + service (`nutrition`)

_The nutrition subsystem's energy/macro math is mostly sound (Mifflin-St Jeor and Katch-McArdle are correct, activity multipliers standard, fat floor and calorie floor enforced, carbs clamped non-negative), but the meal-plan and filtering layer has multiple production-breaking defects that ship silently because the builder never calls validate_day. The two most serious: (1) the religious-restriction filter is completely inert — zero foods in the DB carry any religious_restrictions tag, so a halal/kosher client is served pork; (2) any client with hypertension or type2_diabetes gets an empty food pool (no food carries the required medical_tags), and nutrition_service.generate has no empty-pool guard, so it persists a 7-day plan of 0 kcal / 0 protein. Diet vocabulary is also broken: keto is only a food filter, never a macro adjustment, so keto clients get a ~160g carb target against a zero-grain pool; the dislikes filter is exact-match only; 5/6-meal plans reference slot names no food declares; lunch and dinner are byte-identical every day; the weekly repeat cap counts occurrences not days and is exceeded; the food DB is 68 items (docstring claims ~150) with a planted "egan" typo tag; and there is no imperial-unit support or biometric range validation. The domain math has unit tests; the meal_builder, food_db, filtering, and service have none._

**Coverage:** PRESENT: Mifflin-St Jeor + Katch-McArdle BMR, 5-level activity multipliers with one-tier bias-down, goal surplus/deficit deltas, multi-source calorie floor, protein g/kg per goal (LBM-aware), fat %-of-kcal with 0.8 g/kg floor, carb remainder with non-negative clamp + carb-rescue, fiber/water heuristics, PuLP portion optimizer with naive fallback, cascade food filter, allergen exclusion (works correctly), diet-tag filter, 3/4/5/6-meal splits, check-in calibration, GET /nutrition/plan endpoint. Deterministic output confirmed. MISSING for a real product: (1) NO religious-restriction data on any food -> halal/kosher non-functional, pork served to halal users; (2) NO medical_tags on any food -> hypertension/type2_diabetes -> empty plan; (3) no keto/low-carb GOAL path (keto only filters foods, macro target still carb-heavy); (4) only 68 foods (docstring says ~150), thin coverage: no plant-milks (almond/oat/soy milk), no seitan, no most fish species, no eggs-alternatives, sparse vegan protein for keto-vegan (impossible combo unguarded); (5) no sesame allergen, peanut conflated with tree-nuts under one "nuts" tag (cannot express peanut-only allergy); (6) no imperial units (lbs/inches) and no biometric range validation -> US users and fat-finger inputs produce garbage BMR; (7) validate_day exists but is never invoked, so no plan is ever checked against its targets; (8) no empty-pool / no-plan-possible user messaging.

- 🔴 **critical** [correctness] ✅verified→critical `app/domain/nutrition/meal_builder.py:102-103` — **Religious-restriction filter is inert -> halal/kosher clients served pork**: filter_food_pool excludes a food only if a requested restriction string is FOUND in that food's religious_restrictions list: `[f for f in result if not any(r in f.religious_restrictions for r in religious_restrictions)]`. But in food_db.py NOT A SINGLE FoodItem populates religious_restrictions (every entry passes `[]`). Verified empirically: filter_food_pool(pool, religious_restrictions=['halal']) returns all 68 foods INCLUDING 'Pork Tenderloin'. The semantics are also inverted vs intent: foods should be tagged with what they VIOLATE (e.g. pork tagged 'halal','kosher'), but the data is empty so the filter is a no-op for every religious client. A Muslim or Jewish paying client receives a plan containing pork/shellfish. → _Fix:_ Populate religious_restrictions on non-compliant foods (pork_tenderloin -> ['halal','kosher'], shellfish/shrimp -> ['kosher'], etc.) so the existing exclusion logic fires; add a test asserting no halal-violating food survives the filter. Long term tag every food with explicit halal/kosher compliance.
- 🔴 **critical** [bug] ✅verified→critical `app/services/nutrition_service.py:86-115` — **Hypertension/type2_diabetes clients get a persisted 7-day 0-kcal plan**: filter_food_pool (meal_builder.py:109-114) requires 'low_sodium' in f.medical_tags for hypertension and 'low_sugar' in f.medical_tags for type2_diabetes. NO food in food_db.py sets medical_tags (it defaults to []). Verified: filter_food_pool(pool, medical_conditions=['hypertension']) returns 0 foods; same for type2_diabetes. The hard filter empties the pool and the soft-filter rescue at meal_builder.py:123-125 does not help (the pool is already empty). nutrition_service.generate has NO `if not filtered_pool` guard: it loops build_day_plan 7 times over the empty pool, which returns DayPlan(kcal=0, protein_g=0, slots with 0 items) (verified), then persists a NutritionPlan whose plan_json is 7 days of zero-calorie empty slots while kcal_target/protein_g still show the computed targets. validate_day is imported (line 21) but NEVER called, so nothing catches it. The client is shown an activatable plan of literally no food. → _Fix:_ Tag foods with medical_tags (low_sodium/low_sugar) so the filter has data, OR change the medical filter to a soft/degrade-gracefully step. In nutrition_service.generate add `if not filtered_pool: logger.error(...); return None` and surface a clear message; additionally call validate_day per day and refuse/flag plans that fail.
- 🟠 **high** [correctness] ✅verified→high `app/domain/nutrition/macros.py:29-45, 73-93` — **Keto is only a food filter, never a macro target -> impossible-to-hit ~160g carbs on a zero-grain pool**: diet_style='keto' filters foods to keto-tagged items (verified pool has 31 foods, ZERO grains). But calculate_macros derives carbs purely from goal (fat_loss/maintain/etc.), never from diet_style, and keto is not a goal key. A keto client with goal='maintain' at 1800 kcal/80kg gets protein_g=144, fat_g=64, carb_g=162 (verified). The meal builder must then hit 162g carbs from a pool with no starches, which is physically impossible, so every keto day badly misses macros. Because validate_day is never called, the broken plan ships. Keto/low-carb has no calorie-from-fat re-weighting at all. → _Fix:_ Add a keto/low-carb macro path: when diet_style in ('keto',) cap carbs (e.g. <=50g or <=5% kcal) and reallocate remaining kcal to fat. Make goal and diet_style jointly drive macros, and validate that the filtered pool can supply the carb target before shipping.
- 🟠 **high** [bug] ✅verified→high `app/domain/nutrition/meal_builder.py:284-345` — **5- and 6-meal plans collapse to unbalanced protein-only snacks (slot-name mismatch)**: _MEAL_NAMES for 5/6 meals include 'mid_morning','afternoon_snack','evening_snack' (lines 287-288), but every FoodItem.meal_slots in food_db.py uses only breakfast/lunch/dinner/snack. _pick filters `slot_name in f.meal_slots` (line 330), so for these slots it returns nothing; the `len(selected)<2` fallback (line 340) ALSO filters by `slot_name in f.meal_slots` so still returns nothing; only the final `selected = available[:3]` (line 344) fires, yielding 3 arbitrary same-category foods. Verified: a 5-meal plan's 'mid_morning' slot became ['chicken_breast','chicken_thigh','salmon'] (3 proteins, no carb/veg/fat). The client's mid-morning/afternoon/evening meals are unbalanced protein dumps. → _Fix:_ Either map the extra slot names to real food meal_slots (treat mid_morning/afternoon_snack/evening_snack as 'snack', breakfast, or main), or add those names to the relevant foods' meal_slots. Make _pick fall back to slot-agnostic selection that still balances categories.
- 🟡 **medium** [usability] `app/domain/nutrition/meal_builder.py:328-344` — **Lunch and dinner are identical every single day**: _pick takes the first n candidates deterministically (`candidates[:n]`) with no offset between slots. For the default 3-meal plan, lunch and dinner share the same _MAIN-eligible candidate ordering, so both slots pick the same protein/starch/veg/fat. Verified across a 7-day run: lunch and dinner items were byte-identical every day (e.g. both chicken_breast + white_rice_cooked + broccoli + avocado). A paying client eating the exact same lunch and dinner 7 days a week is a poor product experience even if macros are met. → _Fix:_ Vary selection between same-day slots (e.g. rotate the candidate window per slot index, or exclude items already chosen earlier in the day) and rotate across days using the existing used_slugs map as a deprioritization signal rather than only a hard >5 cap.
- 🟡 **medium** [bug] `app/domain/nutrition/meal_builder.py:312-315` — **Weekly repeat cap counts occurrences, not days, and is exceeded**: Docstrings claim 'no food > 5 days/week' (line 24) and 'prevents >5 uses/week' (line 305). build_day_plan filters `used_slugs_this_week.get(f.slug, 0) < 5`, but nutrition_service.generate increments used_slugs once per slot-occurrence (lines 112-114), and a food like broccoli appears in 3 slots/day. Verified over 7 days: broccoli, avocado, chicken_breast etc. reach count 6 in final used_slugs while still appearing daily. The intended 'max 5 days' semantics is unenforceable because per-day usage already adds 2-3 to the counter, and the cap fires mid-week causing abrupt menu swaps rather than smooth variety. → _Fix:_ Track days-used (increment each slug at most once per day) and compare that to a days threshold, or define the cap in terms of total weekly servings explicitly. Align code with the documented 5-days rule.
- 🟡 **medium** [usability] `app/domain/nutrition/meal_builder.py:106-108` — **Dislikes filter is exact-match only; common inputs silently do nothing**: Dislikes are matched as `f.slug not in dislike_set and f.name.lower() not in dislike_set` against a lowercased set. This requires the user to type the exact slug ('chicken_breast') or the exact full name ('chicken breast (skinless)'). Verified: filter_food_pool(pool, dislikes=['chicken']) leaves both chicken_breast and chicken_thigh in the pool. A client who says they dislike 'chicken' still gets chicken in every meal. No substring/token matching. → _Fix:_ Match dislikes by substring/token against name and category (e.g. exclude if any dislike token is a word in f.name or equals f.category), and/or expose a curated dislike vocabulary in the bot intake so values map to real slugs.
- 🟡 **medium** [usability] `app/bot.py:3161-3178` — **No imperial units and no biometric range validation -> garbage BMR for US/typo inputs**: dn_weight/dn_height only `float()` the raw text and store directly into weight_kg/height_cm; the prompts say kg/cm but there is no imperial (lbs/inches) option anywhere in the codebase (grep for lbs/pound/inch/2.2046/2.54 finds none). A US client entering '180' (meaning lbs) is stored as 180 kg, producing a wildly wrong BMR/TDEE/plan. There is also no range validation: weight=5 or height=20 or age=200 is accepted, and Mifflin-St Jeor (energy.py:59-60) can return very low or even negative BMR for tiny/extreme inputs, which then flows through unchecked. → _Fix:_ Offer a unit toggle (kg/cm vs lbs/ft-in) at intake and convert to metric before storing; add sane range validation (e.g. 30-300 kg, 120-230 cm, 13-100 y) with retry prompts.
- 🟡 **medium** [correctness] `app/services/nutrition_service.py:203-236` — **Goal vocabulary drift: 'strength' supported by macros but treated as maintain by energy; calibrate uses fat-loss shift for recomp**: macros.py recognizes goal='strength' (protein 2.0 g/kg, fat 28% at lines 33,41), and the macros docstring/tests reference it, but energy.apply_goal_adjustment (energy.py:87-95) has NO 'strength' key, so a strength client's TDEE is returned unchanged (verified: apply_goal_adjustment(2500,'strength','aggressive')==2500) — strength silently equals maintain on calories while getting strength macros. 'strength' is also absent from NutritionProfile's documented goal list (fat_loss|lean_bulk|bulk|recomp|maintain in models.py:181). Separately, calibrate_from_checkin (line 223) applies _KCAL_SHIFT_FAT_LOSS (100) for any non-bulk goal including 'recomp', and any unknown/typo goal silently yields a 0% calorie adjustment with no warning. → _Fix:_ Define one canonical goal enum shared by energy.py, macros.py, and NutritionProfile; add 'strength' (or remove it) consistently; have apply_goal_adjustment raise/log on unknown goals instead of returning TDEE unchanged.
- ⚪ **low** [data-quality] `app/domain/nutrition/food_db.py:18` — **Obfuscated diet tag yields 'egan' instead of 'vegan' for chicken_breast**: chicken_breast's diet_tags is built as `["vegan"[1:]+"egan"[1:][:0], "omnivore", "keto", "gluten_free"]`. Evaluated, 'vegan'[1:] = 'egan' and 'egan'[1:][:0] = '' so the first tag is the literal 'egan' (verified; a Counter over the DB shows {'egan':1}). It is inert (nothing queries 'egan', and chicken is correctly excluded from real vegan filters and included in omnivore/keto), so impact is data-quality/maintainability, not user-facing — but it is clearly a planted/obfuscated value with no business reason. → _Fix:_ Replace the expression with the plain list ['omnivore','keto','gluten_free'] (chicken is not vegan). Add a startup assertion that every diet tag is in a known set {vegan,vegetarian,pescatarian,omnivore,keto,gluten_free}.
- ⚪ **low** [content-gap] `app/domain/nutrition/food_db.py:1-15, 139-144` — **Food DB is 68 items (docstring claims ~150); allergen taxonomy conflates peanuts/tree-nuts and omits sesame**: The module docstring says 'Curated food database (~150 items)' but FOOD_DB contains 68 entries (verified len). Coverage is thin: no plant milks (almond/oat/soy milk), no seitan, limited fish, no vegan keto-friendly proteins beyond tofu/tempeh (both soy), making a vegan+keto or soy-allergic-vegan client nearly unfeedable. Allergen taxonomy: peanut_butter is tagged allergen 'nuts' (line 143-144) conflating legume peanuts with tree nuts — a tree-nut-allergic client who can eat peanuts (or vice versa) cannot be expressed; 'sesame' (a top-9 US allergen) is absent entirely. → _Fix:_ Expand the DB toward the documented ~150 with explicit muscle/diet/equipment coverage; split allergens into 'peanut' vs 'tree_nuts', add 'sesame', and tag foods accordingly.
- ⚪ **low** [test-gap] `app/domain/nutrition/meal_builder.py:255-271` — **validate_day divides by target_kcal/target_fat_g with no zero-guard (latent)**: validate_day computes `abs(day.kcal - target_kcal)/target_kcal` and `abs(day.fat_g - target_fat_g)/target_fat_g`; with a 0 target it raises ZeroDivisionError (verified). It is currently latent because validate_day is never called by the service, but if/when validation is wired in (which the empty-pool bug requires), zero or near-zero targets from an empty/edge profile will crash. There are also zero unit tests for meal_builder.py, food_db.py, filter_food_pool, build_day_plan, or nutrition_service.py — only energy/macros math is tested. → _Fix:_ Guard the ratio denominators (treat target<=0 as a hard failure rather than dividing), wire validate_day into generate, and add tests for filtering (halal/medical/dislikes), empty-pool handling, keto, and 5/6-meal balance.

### Exercise database quality (`exercise-data`)

_Exercise DB audit, part 1 of findings._

**Coverage:** Present: 179 exercises, 8 patterns, 16 muscles, 16 equipment types. Missing: no bodyweight-only path for lats, mid_back, biceps, triceps, side_delts, rear_delts, upper_chest, adductors; lower_back has only bb_rack_pull; powerlifter avatar has zero side_delts/rear_delts/calves/adductors options.

- 🟠 **high** [data-quality] ❌REFUTED `app/exercise_db.py:3,73` — **True clone: two identical Incline Dumbbell Fly entries**: db_fly_incline and db_incline_fly identical (upper_chest, dumbbells+bench, lengthened, fatigue 2) except exercise_id. _fill_slots dedups by exercise_id (generator.py:281,432) so both can appear in one week. → _Fix:_ Delete one of the two ids.
- 🟠 **high** [data-quality] ✅verified→low `app/exercise_db.py:42,3` — **True clone: two identical Smith Machine Standing Calf Raise entries**: smith_standing_calf_raise and smith_calf_raise identical (calves, smith_machine, mid_range, fatigue 2) except id. → _Fix:_ Remove smith_calf_raise.
- 🟠 **high** [data-quality] ✅verified→high `app/exercise_db.py:3,55` — **True clone: two identical Single-Leg Leg Extension entries**: leg_extension_machine_single_leg and machine_leg_extension_single_leg identical (quadriceps, leg_extension_machine, shortened, fatigue 2) except id. → _Fix:_ Remove machine_leg_extension_single_leg.
- 🟠 **high** [data-quality] ✅verified→medium `app/exercise_db.py:3,71` — **Duplicate High-to-Low Cable Fly with contradictory fatigue_cost 2 vs 1**: cable_fly_high_to_low fatigue 2 and cable_high_to_low_fly fatigue 1 share name, chest, cable_machine, shortened. Fatigue feeds slot eligibility (generator.py:276) and both can appear via dedup-by-id. → _Fix:_ Delete cable_high_to_low_fly and pick one fatigue value.
- 🟠 **high** [data-quality] ✅verified→medium `app/exercise_db.py:3,72` — **Duplicate Low-to-High Cable Fly with contradictory fatigue_cost 2 vs 1**: cable_fly_low_to_high fatigue 2 and cable_low_to_high_fly fatigue 1 share name, upper_chest, cable_machine, shortened; only fatigue and id differ. → _Fix:_ Delete cable_low_to_high_fly and reconcile fatigue.
- 🟠 **high** [data-quality] ✅verified→low `app/domain/nutrition/food_db.py:18` — **CROSS-SUBSYSTEM string-slice obfuscation yields junk diet tag egan on Chicken Breast**: chicken_breast diet tag is built by slicing string literals (vegan sliced [1:] plus egan sliced [1:][:0]) which evaluates to egan, a junk tag. Chicken gets no real diet flag yet the slice resembles vegan. Only line 17-18 uses it. → _Fix:_ Use the literal list omnivore/keto/gluten_free; never a vegan tag for chicken.
- 🟡 **medium** [data-quality] `app/exercise_db.py:74,3` — **Redundant pair Dumbbell Fly vs Flat Dumbbell Fly**: db_fly_flat and db_flat_fly are the same movement (chest, isolation, lengthened, dumbbells+bench, fatigue 2) under two names. → _Fix:_ Merge into one entry.
- 🟡 **medium** [data-quality] `app/exercise_db.py:81,3` — **Redundant pair Cable Hip Adduction vs Machine Hip Adduction**: cable_hip_adduction and machine_hip_adduction identical (adductors, isolation, lengthened, cable_machine, fatigue 1); machine variant uses no distinct equipment token. → _Fix:_ Give machine_hip_adduction a distinct token or remove it.
- 🟡 **medium** [data-quality] `app/exercise_db.py:3,53` — **Two near-identical pause back squats with conflicting secondary muscles**: bb_pause_back_squat secondary glutes/adductors/core vs bb_pause_squat secondary glutes/hamstrings/lower_back are the same lift with different secondary lists and names. → _Fix:_ Consolidate into one Pause Back Squat entry.
- 🟡 **medium** [correctness] `app/exercise_db.py:3,4-5` — **biomechanical_focus inconsistent for same movement (pull-up vs weighted pull-up)**: bw_pull_up_pronated and bw_chin_up_supinated are mid_range but identical bw_weighted_pull_up and bw_weighted_chin_up are lengthened. bio_focus is a hard filter (generator.py:170) for synergy pairing. → _Fix:_ Pick one bio_focus per movement family.
- 🟡 **medium** [correctness] `app/exercise_db.py:3,41,43` — **Calf-raise biomechanical_focus internally inconsistent**: Most calf raises are mid_range but machine_leg_press_calf_raise and db_single_leg_calf_raise are lengthened. Standing calf raise is stretch-emphasis so most are arguably mistagged. → _Fix:_ Standardize calf raises to lengthened_position.
- 🟡 **medium** [data-quality] `app/exercise_db.py:21,24,27,31,70,81` — **Six machine/pec-deck exercises mislabeled equipment cable_machine**: Machine Chest Press, Machine Incline Chest Press, Machine Lateral Raise, Reverse Pec Deck, Machine Hip Abduction, Machine Hip Adduction list cable_machine but use no cable stack. LATENT: bot defaults equipment to full_gym (bot.py:2032,2047) and generator.py:144 wildcards full_gym. → _Fix:_ Add distinct tokens (pec_deck, chest_press_machine) and gate on them.
- 🟡 **medium** [content-gap] `app/exercise_db.py:n/a` — **No bodyweight-only path for back, arms, shoulders, upper chest, adductors**: Equipment bodyweight only yields 0 exercises for lats, mid_back, biceps, triceps, side_delts, rear_delts, upper_chest, adductors. LATENT for bot users (full_gym wildcard) but live for /generate callers passing bodyweight. → _Fix:_ Add bodyweight rows, dips, band options, or document the limitation.
- 🟡 **medium** [content-gap] `app/exercise_db.py:3 bb_rack_pull` — **lower_back has one exercise, excluded for lower_back_pain users**: Only bb_rack_pull has primary lower_back; needs barbell+squat_rack, powerlifter/powerbuilder only, and generator.py:152 excludes hinge/lower_back exercises for lower_back_pain clients. Also absent from MUSCLE_TO_BUDGET_KEY. → _Fix:_ Add back-extension/hyperextension/reverse-hyper entries incl bodyweight/gen_pop.
- 🟡 **medium** [data-quality] `app/exercise_db.py:n/a` — **core, adductors, lower_back not budgeted**: MUSCLE_TO_BUDGET_KEY (generator.py:49-63) omits core, adductors, lower_back. _budget_key returns None via .get (185) and budget.get(None,0)=0. Graceful but 13 core, 4 adductor, 1 lower_back exercises count zero toward set caps. → _Fix:_ Add these keys to MUSCLE_TO_BUDGET_KEY and _budget_volume.
- 🟡 **medium** [content-gap] `app/exercise_db.py:n/a` — **Powerlifter avatar has zero side_delts/rear_delts/calves/adductors options**: No powerlifter-tagged exercise has primary side_delts, rear_delts, calves, or adductors. The powerlifter Accessory/GPP Day (generator.py:88) targets these and _select_for_slot returns None (280-310), silently emptying the slot. → _Fix:_ Add powerlifter tag to a few delt/calf isolations or fall back to gen_pop pool.
- ⚪ **low** [data-quality] `app/exercise_db.py:3` — **Phantom secondary muscle shoulders duplicates the primary**: db_lateral_raise, cable_lateral_raise, db_front_raise, cable_front_raise, cable_pallof_press list shoulders as secondary, but shoulders is never a primary and has no budget key. → _Fix:_ Remove shoulders or replace with a specific delt head.

### Telegram bot flows (`bot`)

_The Telegram bot (app/bot.py, 5184 lines; auth in app/auth/roles.py) is a mature, mostly well-defended system: the role decorators (requires_active_sub, requires_assigned_coach) ARE applied to client commands, /review and /override do inline super-admin/coach role checks, the workout approve/reject/feedback handlers consistently re-check _user_can_act_on_client for horizontal-privilege scoping, the coach picker verifies chat ownership, pay-verify and pay-reject guard on payment.status to block double-processing, and the error handler is carefully deduplicated. The stale CLAUDE.md claims (test failures, missing slot_type) do not reflect the live code. The real problems are a cluster of privileged state-change callback handlers that were never given the authz/scope check their siblings have: handle_nutrition_approve/discard (no role check at all, unlike the workout path it mirrors), and handle_safety_clear / handle_override_remove / handle_fc_confirm / handle_fc_edit (rely entirely on button distribution for security, with zero runtime gate). Secondary issues: a raw engine exception string is leaked to the client, the client intake email is stored without validation (inconsistent with the two other email entry points that validate), /log accepts out-of-range/negative weight and RPE, and the 24h-acknowledgment guard references a dead user_data key so the nudge can interleave into an active /diet flow. The empty app/adapters/telegram/ tree is dead scaffolding — bot.py is the entire implementation._

**Coverage:** Flows audited and present: client intake (/start → pre-pay menu → subscribe/FAQ/login/coach-apply), pay screenshot → admin verify/reject, coach application (8 steps) → admin approve/reject, coach picker (self-pick + admin-assign), profile update (/update_profile), check-in (structured per-lift + free-text + resume), manual /log, nutrition intake (/diet, 18 steps), workout approve/reject/LLM-edit, form-check (tips + video reply routing), 24h ack nudge, renewal-reminder + expiry jobs. Auth model: super-admin (env), coach (CoachProfile.status=approved, 60s cache), authenticated client (ChatBinding→ClientProfile), subscription gate. MISSING for a real product: (1) diet style coverage exists for vegan/vegetarian/pescatarian/keto/balanced/omnivore and captures allergies/dislikes/religious(halal,kosher) as free text, but the bot never validates allergen/religious strings against any list and passes them downstream as raw tokens — no guarantee the meal builder honors them (cross-subsystem). (2) No biometric range validation at the bot boundary (weight/height/age/bodyfat accept 0, negatives, or absurd values). (3) No way for a client to view/cancel a subscription, see remaining days, or get a payment receipt/refund path. (4) No /diet quick exit or restart, and no handler for unexpected text in callback-only states (intake avatar/days/experience, coach specialty, all dn_* button steps) — typing instead of tapping yields silence. (5) No re-apply path for rejected coaches (hard dead-end pointing to "reach out"). (6) Equipment is hardcoded to ["full_gym"] at intake with no way to set home/limited equipment via the bot.

- 🟠 **high** [security] ✅verified→high `app/bot.py:3509-3597` — **handle_nutrition_approve / handle_nutrition_discard have no authorization or scope check**: Both handlers are registered as standalone global CallbackQueryHandlers (lines 5140-5141, patterns ^nutrapprove: and ^nutrdiscard:) and neither performs any role or scope check. handle_nutrition_approve does `plan_id = int(query.data.split(':')[1])`, then activates the NutritionPlan, supersedes prior active plans, renders a PDF and DMs it to the client — with no is_super_admin / is_coach / _user_can_act_on_client gate. This is in direct contrast to the workout sibling handle_admin_approve (line 3754) and handle_open_pending_item (4289), which both call `_user_can_act_on_client(update.effective_user.id, pending.client_id)` before acting. plan_id is a sequential integer (NutritionPlan.id autoincrement), so among the bot's own coaches there is no horizontal-privilege isolation: coach A can approve/discard coach B's client's nutrition draft by acting on any plan id. handle_nutrition_discard likewise sets status='rejected' on any plan id with no check. → _Fix:_ Add the same gate the workout path uses at the top of both handlers: load the NutritionPlan, then `if not _user_can_act_on_client(update.effective_user.id, plan.client_id): await query.edit_message_text('🔒 Not authorized for this client.'); return`. Reject the action and log coach_scope_violation, mirroring handle_admin_approve.
- 🟠 **high** [security] ✅verified→high `app/bot.py:4787-4809` — **handle_safety_clear bypasses a medical safety gate with no authorization check**: handle_safety_clear is a global CallbackQueryHandler (line 5145, pattern ^safety_clear:) that, on callback `safety_clear:<client_id>:<condition_key>`, writes profile.safety_override_note = 'Cleared by physician for ...' — permanently disarming the SafetyRefusalError gate (raised in run_generation_and_dispatch line 559) for that condition so the next plan generates unchecked. The handler does NO role check whatsoever (no is_super_admin, no is_coach, no _user_can_act_on_client). The clearance button is only ever sent to _admin_chat_id() (line 562), so in normal operation only the admin can press it, but the handler relies entirely on button distribution for security: there is zero runtime gate on a medical-safety bypass. Given that overriding a physician-clearance gate is the single most safety-critical action in the bot, it must not depend on UI distribution alone. → _Fix:_ Gate the handler: `if not (auth_roles.is_super_admin(update.effective_user.id) or _user_can_act_on_client(update.effective_user.id, client_id)): await query.edit_message_text('🔒 Not authorized.'); return`. Restrict to super-admin or the client's assigned coach, and log who cleared the gate.
- 🟡 **medium** [security] `app/bot.py:4764-4782` — **handle_override_remove has no authorization or scope check**: handle_override_remove (global CallbackQueryHandler, line 5144, pattern ^override_remove:) parses `override_remove:<client_id>:<from_id>` and removes a coach exercise override on any client_id with no role/scope check, while its sibling handle_override (line 4690) carefully gates set/list operations on is_super/is_coach AND profile.assigned_coach_id == user_id (line 4717). The Remove buttons are only emitted inside the role-gated handle_override (line 4736), so in normal operation only an authorized coach sees them, but the handler itself relies entirely on button distribution — a coach who can produce/replay another coach's client_id+from_id would mutate a client they are not assigned to. Inconsistent authz between the two halves of the same feature. → _Fix:_ Mirror handle_override's check before deleting: resolve the profile and `if not _user_can_act_on_client(update.effective_user.id, client_id): await query.edit_message_text('🔒 Not authorized for this client.'); return`.
- 🟡 **medium** [security] `app/bot.py:583-586` — **Raw engine exception string leaked to the client**: In run_generation_and_dispatch the generic except block sends the raw exception to the CLIENT: `text=f"Oops! Something went wrong in the engine: {exc}"` (line 585). Any unexpected error — DB error, validation error, LLM/SDK error — surfaces its internal message (potentially containing table names, file paths, SQL fragments, or API error bodies) directly to the end user. Everywhere else the bot keeps tracebacks admin-only (handle_error, line 4426). This is an information-disclosure and poor-UX path on a paying client's first plan. → _Fix:_ Log the exception with logging.exception and send the client a generic message ('Something went wrong generating your plan — your coach has been notified.'), routing the detail to the admin via the existing handle_error/_admin_chat_id channel.
- 🟡 **medium** [data-quality] `app/bot.py:1998-2000` — **Client intake email is stored with no format validation (inconsistent with the other two email entry points)**: handle_email does `email = update.message.text.strip(); context.user_data['email'] = email` and persists it to ClientProfile.email with no validation. A helper `_looks_like_email` exists (line 102) and IS used by coach_apply_email (line 1172) and upd_set_email (line 2325), but NOT here. So a client can complete intake with 'asdf' as their email. The bot delivers plans via Telegram send_document (not email), so this is data-quality rather than a delivery failure — but the stored value is shown to the admin (line 306) and email presence is used as a proxy for 'profile set up' (line 1543, `if not (client.email and client.training_days)`), so an invalid string both pollutes the coach's view and can mis-drive the setup-completion branch. The inconsistency with the two validated entry points is the core defect. → _Fix:_ Add the same guard at the top of handle_email: `if not _looks_like_email(email): await update.message.reply_text('That doesn\'t look like an email. Please send a valid one.'); return ASK_EMAIL`.
- 🟡 **medium** [correctness] `app/bot.py:4582-4601` — **/log accepts negative / out-of-range weight and RPE with no bounds check**: handle_log_weight (line 4582) accepts any float as weight — `context.user_data['log_weight'] = float(text)` — with no positivity or upper-bound check; handle_log_rpe (line 4594) prompts '1–10' but accepts any float (e.g. -5, 0, 99). These land unsanitized in slot.actual_weight / slot.actual_rpe (lines 4613-4615) and are persisted to WorkoutHistory, then feed the AutoRegulator's next-week load calc (4% per RPE point off actual_weight). A negative or zero weight produces a nonsense target_weight. Note: the structured /checkin path validates RPE 1-10 (handle_structured_rpe line 2618) but NOT weight (handle_structured_weight line 2591 accepts any float), so the weight gap exists in both paths; the /log path is the airtight one (direct write, no LLM layer). → _Fix:_ Validate ranges in handle_log_weight (e.g. 0 < weight <= 600) and handle_log_rpe (1 <= rpe <= 10), and add the same `0 < weight` check to handle_structured_weight; re-prompt on out-of-range input instead of accepting it.
- ⚪ **low** [bug] `app/bot.py:4629` — **24h acknowledgment guard references a dead user_data key, so the nudge interleaves into the active /diet flow**: check_plan_acknowledgment is registered at group=-1 to run on every client text message (line 5163). Its suppress-during-active-flow guard is `_active_conversation_keys = {'checkin_history_id', 'log_history_id', 'avatar', 'dn_weight'}` (line 4629). But the nutrition flow stores all of its data under the key 'dn_data' (via _dn(), line 3118-3121) and never sets 'dn_weight'. So during an in-progress /diet conversation the guard never matches, and the 24h 'how's your plan feeling?' nudge can fire mid-flow (group=-1 runs alongside the group-0 diet handler). The diet conversation itself isn't broken, but the suppression intent is silently defeated for the longest intake flow in the bot. → _Fix:_ Replace 'dn_weight' with 'dn_data' in _active_conversation_keys so the guard matches the actual key the nutrition flow sets.
- ⚪ **low** [correctness] `app/bot.py:944-1027` — **pay-verify is idempotent on double-tap but not race-safe on concurrent verify**: handle_payment_verify guards on `if payment.status != 'pending': return` (line 949), which correctly blocks sequential double-taps (idempotent for the common case). However the read-check-write is not atomic (no SELECT FOR UPDATE / row lock), so two concurrent verifies of the same payment could both read status='pending' and both proceed to create a Subscription + AccessCode. The AccessCode primary key is client_id (models.py line 330), so the second AccessCode insert would raise an IntegrityError and roll back its transaction — which partially mitigates — but Subscription.payment_id has no unique constraint (models.py line 324), so a duplicate Subscription could be created if the timing splits before the access-code insert. Low likelihood with a single admin pressing once, but there is no DB-level guarantee against duplicate subscriptions per payment. → _Fix:_ Add a unique constraint on Subscription.payment_id (one subscription per verified payment) and/or perform the status flip with a conditional UPDATE (UPDATE payment SET status='verified' WHERE id=? AND status='pending') checking rowcount before creating the subscription.
- ⚪ **low** [security] `app/bot.py:3675-3716` — **handle_fc_confirm / handle_fc_edit (form-tip send) have no authorization check**: handle_fc_confirm (line 3675, global handler 5139) and handle_fc_edit (line 3699, conversation entry 5026) act on `fc_confirm_<uuid>` / `fc_edit_<uuid>` to send coach-reviewed technique tips to a client, with no is_super_admin/is_coach check — they only look up the uuid in bot_data['pending_tips']. The buttons are sent only to _admin_chat_id() (line 3030), so exploitability is low (the uuid is a random uuid4 not exposed to clients, and the payload is benign tips), but like the nutrition handlers these privileged 'send to client' actions rely entirely on button distribution rather than a runtime role gate. → _Fix:_ Add an is_super_admin-or-is_coach guard at the top of handle_fc_confirm and handle_fc_edit, consistent with the other admin callbacks.
- ⚪ **low** [usability] `app/bot.py:4896-4903` — **Callback-only intake/nutrition states give no feedback when the user types instead of tapping**: Several conversation states accept only CallbackQueryHandlers and have no MessageHandler fallback: ASK_AVATAR/ASK_DAYS/ASK_EXPERIENCE (4896-4898), coach specialty (4933), and every dn_* button step (dn_sex, dn_goal, dn_aggressiveness, dn_activity, dn_diet_style, dn_meals, dn_cooking_skill/time, dn_budget — 5067-5095). If the user types text instead of tapping the inline button, nothing in the conversation handles it, the state does not change, and the user gets silence (only /cancel works). ASK_LIMITATIONS does include a MessageHandler (4902) showing the pattern is feasible. This produces a confusing 'stuck' feeling for users who reply by typing. → _Fix:_ Add a catch-all MessageHandler(filters.TEXT) to each button-only state that re-sends the keyboard with a 'Please tap one of the buttons above.' hint, or accept equivalent typed input.
- ⚪ **low** [data-quality] `app/bot.py:3161-3196` — **Nutrition biometrics accept zero, negative, and absurd values at the bot boundary**: dn_weight (3161), dn_height (3171), dn_age (3181), and dn_bodyfat (3211) only catch ValueError on float()/int() conversion — they accept weight_kg=0 or -50, height_cm=0, age=0 or 500, body_fat_pct=150. These flow into calculate_bmr / calculate_tdee (app/domain/nutrition/energy.py) where, e.g., body_fat_pct>100 makes lbm_kg = weight*(1 - bf/100) negative. The energy module has lower calorie floors but no input sanity gate, and the bot is the only validation boundary. (The downstream macro/meal-builder consequences belong to the nutrition subsystem; flagged here as the missing boundary validation in the bot intake.) → _Fix:_ Add range checks in each dn_* numeric handler (e.g. 25<=weight<=400, 90<=height<=250, 13<=age<=100, 3<=bodyfat<=60) and re-prompt on out-of-range values before storing.

### FastAPI web API + auth (`api-web`)

_The FastAPI web API + auth subsystem is reasonably well-structured: the v1 routers (plans, profile, progress, sets, checkin, coach, admin, feedback, nutrition) consistently scope queries by user.client_id and the coach/admin ownership checks (client.coach_id == user.client_id, _require_super_admin) are correct, so read-side IDOR is largely absent. However there are real auth defects. The documented CoachedWorkoutResponse field mismatch is STALE/FALSE — the model field is `workout` and routes.py returns `workout=`, they agree. Live defects: (1) the JWT default secret is `change-me-in-production`, so anyone who knows the default can forge an access token for any client_id and take over every account; (2) `decode_token` (the access-token verifier) accepts ANY valid JWT regardless of `type`, so emailed reset/verify tokens and refresh tokens all function as full Bearer access tokens; (3) the legacy `/generate` and `/generate_and_coach` routes are mounted with no authentication and accept an arbitrary ClientProfile body; (4) `update_profile` does setattr-bypass with no validation; (5) GDPR delete leaves PII (email/name) in ProfileSnapshot and Feedback; (6) a write-IDOR in /sets lets a user attach set logs to any history_id; (7) reset tokens are reusable and refresh tokens are never invalidated. Content/usability gaps exist around weak registration password rules and missing rate-limit coverage on some auth routes._

**Coverage:** Covered: every file in app/api/*.py, app/auth/*.py, app/main.py, app/routes.py, app/container.py, plus referenced models (ClientProfile, SetLog, Feedback, ProfileSnapshot, CoachedWorkoutResponse) and settings. Authorization on v1 routers is genuinely solid: coach endpoints verify client.coach_id == user.client_id on every approve/reject/edit/detail call; admin endpoints gate on is_admin / super_admin_email; per-user data routes filter all reads by user.client_id (no cross-user read IDOR found in plans/progress/checkin/nutrition/feedback GETs). MISSING for a real product: (a) no token revocation / blocklist — stateless JWT with no jti, so /logout cannot kill a stolen bearer token, reset links are replayable within TTL, and old refresh tokens stay valid after rotation; (b) no email format / password strength validation at registration (only /reset enforces min-8); (c) rate limiting only covers /login,/register,/forgot,/reset — /refresh and /resend-verification (both email/token-spend paths) are unprotected; (d) /login short-circuits before bcrypt for unknown emails, a timing oracle for account enumeration despite /forgot being hardened; (e) GDPR deletion is incomplete (PII survives in two tables and is still exported); (f) input validation gap lets clients push the deterministic generator into degenerate states (training_days out of 3..6, unknown avatar/experience). The CoachedWorkoutResponse "field mismatch" and the test-failure claims in CLAUDE.md are stale and do not reproduce.

- 🔴 **critical** [security] ✅verified→critical `app/settings.py:58` — **JWT signing secret defaults to a publicly-known placeholder**: auth_secret_key defaults to "change-me-in-production" (settings.py:58) and jwt.py:_secret_key() reads this value to sign/verify ALL tokens with HS256. main.py:152 only WARNS at startup if the default is in use — it does not refuse to boot. If a deployment ships with the default (or any leaked secret), an attacker computes jwt.encode({'sub':'cl_victim','exp':...}, 'change-me-in-production', algorithm='HS256') and get_current_user (deps.py:36-42) accepts it as that user — full account takeover for every client, coach, and admin (admin status is derived from the resolved ClientProfile row). → _Fix:_ Refuse to start when auth_secret_key is empty or equals the placeholder (raise in lifespan instead of logger.warning), and require AUTH_SECRET_KEY to be set with no default in production. Fail closed, not open.
- 🟠 **high** [security] ✅verified→high `app/auth/jwt.py:41-47` — **Access-token verifier accepts refresh/reset/verify tokens (token type confusion)**: decode_token() decodes any HS256 token and returns its `sub` without checking the `type` claim, unlike decode_refresh_token / decode_reset_token / decode_verify_token which all enforce `type`. get_current_user (deps.py:36) authenticates with decode_token. Empirically confirmed: create_refresh_token('cl_victim'), create_reset_token('cl_victim'), and create_verify_token('cl_victim') all decode successfully through decode_token and authenticate as that user. Reset and verify tokens are emailed to users (auth.py:88-92, 149-153) and therefore land in inboxes, mail-server logs, and support tickets — any leaked one becomes a full API access token for its TTL (30 min reset, 48 h verify, 30 day refresh). test_api.py:120 tests the reverse direction (refresh path rejects access token) but nothing tests this direction. → _Fix:_ Give access tokens an explicit type claim (e.g. "access") in create_access_token and have decode_token reject any token whose `type` is not "access". Add a test asserting reset/verify/refresh tokens are rejected by get_current_user.
- 🟠 **high** [security] ✅verified→high `app/routes.py:28-55` — **/generate and /generate_and_coach are unauthenticated and accept arbitrary ClientProfile bodies**: Both POST routes take `client: ClientProfile` straight from the request body with no Depends(get_current_user) and no auth, and they are mounted via app.include_router(router) in main.py:243 (no auth prefix/dependency). /generate_and_coach (routes.py:35-55) writes a ProfileSnapshot row with the attacker-supplied client_id (line 44) AND calls the paid LLM (line 53). test_api.py:40 posts to /generate_and_coach with no auth header, confirming this is the live behaviour. This is an unauthenticated cost-amplification + DB-pollution vector (an attacker can spam LLM calls and insert ProfileSnapshot rows under any client_id they choose). → _Fix:_ Either gate these routes behind get_current_user (and ignore the body's client_id, using the authenticated user), or remove them entirely now that /api/v1/plans/generate is the real path. At minimum require auth before the LLM call and snapshot write.
- 🟠 **high** [security] ✅verified→high `app/api/profile.py:93-125` — **Account deletion leaves recoverable PII (email/name) in ProfileSnapshot and Feedback**: delete_my_account anonymises only the ClientProfile row (clears name/email/password_hash) and deletes PendingApproval. It never touches ProfileSnapshot.snapshot_json — which is a full JSON-serialised ClientProfile including email and name (models.py:79, written by routes.py:44 and the generator) — nor Feedback.email, which feedback.py:33 copies from user.email into its own column. Both tables are still returned verbatim by GET /profile/export (profile.py:74,73,87). The docstring also claims it "scrambles client_id linkage" but the code never modifies client_id. So a GDPR deletion request leaves the user's email recoverable in at least two tables and exportable. → _Fix:_ On delete, also delete or scrub ProfileSnapshot rows for the client_id and null/redact Feedback.email (and any other PII-bearing tables). Fix or remove the false "scrambles client_id linkage" docstring claim.
- 🟡 **medium** [correctness] `app/api/profile.py:42-54` — **update_profile bypasses model validation, allowing degenerate generator inputs**: update_profile loops over the client-supplied dict and calls setattr(user, key, val) for any key in _UPDATABLE with no value validation. SQLModel/Pydantic field constraints (training_days Field(ge=3, le=6), models.py:38) are only enforced at construction, NOT on attribute assignment, so a client can PUT {"training_days": 99} or {"training_days": 0}, {"avatar": "garbage"}, {"experience_level": "x"}, or {"available_equipment": "not-a-list"}. These flow into the deterministic generator on the next /plans/generate: _resolve_split (generator.py:97-98) falls through to N generic days (e.g. 99 or 0 days), _budget_volume silently treats any unknown experience as "advanced" (generator.py:107-108), and an unknown avatar makes _filter_exercises (generator.py:139) reject every exercise → empty/failed plan. A 0/negative training_days yields a 0-day plan that /plans/today (plans.py:194) cannot render. → _Fix:_ Validate each update against an allow-list of values/ranges (avatar in {gen_pop,powerlifter,powerbuilder}, 3<=training_days<=6, experience_level in {beginner,intermediate,advanced}, available_equipment is a list of known equipment) and 422 on bad input, or re-validate via ClientProfile.model_validate before persisting.
- 🟡 **medium** [security] `app/api/sets.py:26-46` — **log_set write-IDOR: history_id is not verified to belong to the caller**: log_set stores a SetLog with body.history_id (sets.py:38) without checking that history_id corresponds to a WorkoutHistory owned by user.client_id. test_api.py:253 documents this explicitly: '"history_id": 999, # synthetic — endpoint doesn't verify it exists'. The row is tagged with the caller's own client_id, so the GET (sets.py:57) filters cross-user reads out, but a user can still create set-log rows referencing another user's history_id (data pollution / integrity, and if the autoregulator ever joins SetLog by history_id it could read attacker-injected data) and can fabricate logs against non-existent history_ids with no error. → _Fix:_ Before insert, look up WorkoutHistory by body.history_id and 404/403 unless its client_id == user.client_id. Reject day_index/slot_index/set_index that exceed the plan's actual shape.
- 🟡 **medium** [security] `app/api/auth.py:158-176` — **Reset tokens are replayable and refresh tokens are never invalidated (no revocation)**: reset_password (auth.py:158-176) sets the new password but never consumes/invalidates the reset token; decode_reset_token only checks exp + type, so the same emailed reset link works repeatedly for the full 30-minute TTL — anyone who later obtains that link can re-take the account. refresh (auth.py:112-131) issues a fresh refresh token but does not invalidate the presented one, so it is not true rotation: a stolen 30-day refresh token stays valid even after the user 'rotates'. logout (auth.py:134-139) only clears cookies; a captured Bearer token remains valid until expiry. There is no jti/blocklist or token-version anywhere, so password reset / logout do not actually terminate active sessions. → _Fix:_ Add a token-version/password-changed-at column to ClientProfile; bump it on reset and embed it in tokens so old access/refresh/reset tokens fail verification. Implement single-use reset tokens (store a consumed flag or rotate the version on use) and rotate-on-use refresh tokens with old-token invalidation.
- 🟡 **medium** [usability] `app/api/auth.py:55-97` — **Registration accepts weak/empty passwords and unvalidated emails**: register (auth.py:55) and the schema RegisterRequest (schemas.py:6-9, email/password/name are bare str) enforce no password length or complexity and no email format — only /reset enforces len>=8 (auth.py:162). So a user can register with password "" or "a", and with an email that contains no "@". This is an asymmetry (reset is stricter than register) and a real usability/security hole for a paying product: trivially guessable passwords are accepted at the front door. → _Fix:_ Use pydantic EmailStr for the email field and enforce a minimum password length/complexity (mirror the >=8 used in /reset) in RegisterRequest, returning 422 on violation.
- ⚪ **low** [security] `app/auth/jwt.py:23-28` — **No bcrypt 72-byte cap → passwords differing only past byte 72 are treated as equal**: hash_password/verify_password use passlib bcrypt with no length cap and registration imposes no max length. bcrypt silently truncates at 72 bytes; confirmed empirically that verify('x'*72 + 'DIFFERENT') returns True against hash('x'*100). Combined with the missing registration length check, a user who sets a >72-char password gets weaker-than-expected uniqueness. Separately, the environment shows a passlib/bcrypt version mismatch ('error reading bcrypt version') that should be pinned to avoid future verify breakage. → _Fix:_ Enforce a max password length (e.g. 72 chars) at registration, or pre-hash (sha256) before bcrypt, and pin compatible passlib+bcrypt versions in pyproject.
- ⚪ **low** [security] `app/api/auth.py:100-104` — **Login timing oracle enables account enumeration despite hardened /forgot**: login (auth.py:102-104) returns immediately for an unknown email without running verify_password, but runs the (slow) bcrypt verify when the email exists. This timing difference lets an attacker enumerate which emails are registered — undermining the deliberate anti-enumeration design of /forgot (auth.py:144-155). Also /refresh and /resend-verification are not in the rate-limited path list (main.py:203-204), so token-spend endpoints are unthrottled. → _Fix:_ Run verify_password against a dummy hash when the user is not found so the response time is constant, and add /refresh + /resend-verification to the rate-limited path set.
- ⚪ **low** [usability] `app/api/auth.py:229-246` — **Diagnostic /whoami endpoint shipped in production exposes auth-source internals**: The /auth/whoami route (auth.py:229) is described in its own docstring as a 'Diagnostic endpoint' that reveals exactly how the server resolved the token (has_bearer_header, has_access_token_cookie) plus is_super_admin. It requires auth so it is not a data leak across users, but diagnostic surface like this is normally gated behind a debug flag and is duplicate of /me. → _Fix:_ Gate /whoami behind a DEBUG/diagnostic setting or remove it; /me already returns the equivalent identity payload.
- ⚪ **low** [correctness] `CLAUDE.md:n/a` — **Documented CoachedWorkoutResponse field mismatch is STALE/FALSE**: CLAUDE.md claims 'CoachedWorkoutResponse has a field workout_data but the route returns workout=' . Live code disproves this: models.py:276-278 defines the field as `workout: WorkoutWeek` and routes.py:55 returns CoachedWorkoutResponse(workout=workout, ...). They match; there is no mismatch and the response_model validation passes. The related claims about test failures in CLAUDE.md likewise do not reproduce. Reported only to correct the stale audit baseline. → _Fix:_ Delete the stale 'Key design constraints / known gaps' bullets about CoachedWorkoutResponse and the test_api client_id assertion from CLAUDE.md.

### Models, DB, settings, migrations (`data-models`)

_Subsystem coherent and tests pass._

**Coverage:** PRESENT: full model set, 19 migrations, clean coach split. WEAK: no migration path in bot Postgres deploy.

- 🟠 **high** [bug] ❌REFUTED `app/database.py:9-20` — **create_all defeats alembic**: create_all at import builds Postgres schema before alembic runs; migrations never apply and future model columns never reach prod. → _Fix:_ Gate create_all to SQLite only.
- 🟡 **medium** [data-quality] `app/models.py:78,158,173,203` — **client_id FKs missing from models**: Migrations declare FK to clientprofile.client_id on several tables but model fields are index-only, so create_all omits the FKs. → _Fix:_ Add foreign_key clientprofile.client_id to those model fields.
- 🟡 **medium** [data-quality] `app/models.py:302-351` — **SaaS tables have no client_id FK**: Payment, Subscription, AccessCode, ChatBinding and CheckIn have client_id with no FK in model or migration 0017, so deleting a client orphans rows. → _Fix:_ Add foreign_key clientprofile.client_id with ON DELETE plus migration.
- 🟡 **medium** [bug] `app/main.py:164,177-180` — **Two-engine split-brain**: Lifespan engine from settings vs module engine from os.getenv in auth/roles.py, deps.py, health.py; a divergent url splits the bot auth path from handlers. → _Fix:_ Unify on one engine from get_settings().database_url.
- 🟡 **medium** [data-quality] `alembic/versions/0017_subscription_and_roles.py:38-58` — **0017 DELETEs all client data**: upgrade DELETEs eleven tables including clientprofile and payments as test data, destroying prod data if alembic runs on a populated DB. → _Fix:_ Remove or guard the DELETE behind an env flag and row-count abort.
- 🟡 **medium** [security] `app/settings.py:58` — **Default auth_secret_key only warned**: auth_secret_key defaults to change-me-in-production, guarded only by a warning, so FastAPI deploys on the default have forgeable HS256 tokens; bot-only compose not exposed. → _Fix:_ Raise on the default outside dev; generate a per-deploy secret.
- ⚪ **low** [security] `app/settings.py:61` — **super_admin_email hardcoded with auto-promotion**: super_admin_email defaults to a personal address force-promoted each startup, so on the default anyone registering it becomes super-admin. → _Fix:_ Require SUPER_ADMIN_EMAIL explicitly.
- ⚪ **low** [data-quality] `app/models.py:81` — **ProfileSnapshot.created_at drift**: created_at is nullable False with now default in migration 0002 but Optional None in the model, so alembic and create_all diverge on NULL. → _Fix:_ Pick one source of truth for nullability and default.
- ⚪ **low** [correctness] `app/bot.py:982-990` — **Subscription tz-aware datetimes into a naive column**: bot.py:982-990 writes tz-aware datetimes into a naive column while readers use naive-UTC via _utc_naive_now at bot.py:1712; harmless on Postgres but inconsistent. → _Fix:_ Use _utc_naive_now for Subscription datetimes.
- ⚪ **low** [correctness] `app/database.py:9` — **DATABASE_URL read by two config layers**: database.py:9 reads DATABASE_URL via os.getenv while settings.py:28 reads it via pydantic-settings; drift risk and the module engine ignores settings. → _Fix:_ Derive the DB URL from get_settings().database_url.

### PDF rendering + email dispatch (`pdf-email`)

_The PDF/email subsystem has two parallel PDF systems: the live professional renderer (app/adapters/pdf/renderer.py + Jinja templates), wired into the bot's workout-approve (bot.py:3856) and nutrition-approve (bot.py:3547, handler registered at bot.py:5140) flows, and a legacy markdown PdfService used only as a fallback. The WORKOUT PDF path renders correctly end-to-end (verified with real WorkoutWeek shapes; all CSS classes exist). The NUTRITION PDF path is COMPLETELY BROKEN: two independent template bugs (meal_card's dict.items footgun and shopping_list's total_grams/total_g key mismatch) each crash with TypeError against the real nutrition_service plan_json shape, so render_plan_pdf always raises, the bot swallows it (3554), and every nutrition client receives only a text fallback ("Ask your coach for details") with zero macros/meals. The 230-test suite passes only because tests/test_pdf.py uses an unrealistic fixture (nutrition days with no 'slots'/'items' keys and an empty shopping list) that structurally dodges both bugs. Autoescape is silently OFF for all .html.j2 templates (a latent XSS exposure, currently unreached since only DB/opaque-token fields render). EmailService.send_plan is dead code (never called; live delivery is Telegram send_document). Several lower-severity polish/robustness issues round out the list._

**Coverage:** COVERED: renderer.py (context builders, CSS loader, charts, QR, render_plan_pdf, default_pdf_path); all 14 Jinja templates (base, plan, cover, how_to_read, block_overview, week, day, glossary, nutrition_overview, meal_plan, shopping_list, _exercise_card, _meal_card); pdf_service.py; email_service.py (all 4 senders + their API callers). Each suspected template crash was empirically reproduced via inline Jinja render with the exact shapes from nutrition_service.py (plan_json) and models.py (WorkoutWeek/WorkoutSlot). Reachability traced: workout-approve and nutrition-approve handlers both registered and live; send_plan confirmed dead. CSS: bounded check confirmed every class used by the live workout templates (.exercise-card, .week-grid, .week-cell, .ptile, .info-table, .callout, .badge, .day-header, .prescription-tiles, .warmup-sets, .watermark, running-header) exists in css/*; full WeasyPrint pixel-layout NOT rendered (WeasyPrint not exercised in CI per the test file's own note) — visual layout breakage (overflow, page-break correctness, grid wrapping) is therefore UNVERIFIED and out of scope here. NOT ASSESSED: actual SMTP wire behavior (no live server); whether nutrition food/diet content itself is complete (that is the nutrition subsystem's domain, not delivery). MISSING for a real product: no retry/queue if Telegram send_document throws on the nutrition path after status is already committed active (plan persists but delivery silently lost with no recovery); no per-plan attachment filename; no email address validation anywhere.

- 🟠 **high** [bug] ✅verified→high `app/adapters/pdf/templates/partials/_meal_card.html.j2:14` — **Meal card crashes on every real nutrition plan (dict.items footgun) — nutrition PDF never delivered**: Line 14: `{% for food, grams in slot.items %}`. `slot` is a plain dict (parsed from NutritionPlan.plan_json), so Jinja resolves `slot.items` to the bound dict.items METHOD (attribute lookup precedes subscript), not the 'items' list. Iterating a method object raises `TypeError: 'builtin_function_or_method' object is not iterable`. Verified by rendering this partial with the exact slot dict that nutrition_service.py (lines 150-172) emits — it crashes. Even if it reached the list, items are 8-key dicts (name/slug/category/grams/kcal/...) and would fail tuple-unpack into (food, grams) with `ValueError: too many values to unpack`. Because render_plan_pdf wraps the whole render, this raises, and the nutrition-approve handler (bot.py:3554) catches it, sets pdf_bytes=None, and sends the client a text-only fallback ('Ask your coach for details') — so EVERY nutrition client gets zero meal/macro content. The 230 tests pass because tests/test_pdf.py:_make_nutrition_plan() builds plan_json days with NO 'slots' key, so this loop never executes. → _Fix:_ Use subscript and a single loop var matching the real shape: `{% for item in slot['items'] %}` then render `{{ item.name }}` and `{{ item.grams | round | int }} g`. Replace lines 16-17 accordingly (item.name, item.grams).
- 🟠 **high** [bug] ✅verified→high `app/adapters/pdf/templates/sections/shopping_list.html.j2:14` — **Shopping list crashes on real plan — reads item.total_g but renderer builds total_grams**: Line 14: `{{ item.total_g | round | int }} g`. The renderer's _build_nutrition_context (renderer.py lines 238-242) builds each shopping-list item with key `total_grams`, not `total_g`. So `item.total_g` is Jinja Undefined, and `Undefined | round` raises `TypeError: type Undefined doesn't define __round__ method`. Verified: rendering this section with the dict that _build_nutrition_context actually produces (keys name/slug/total_grams) crashes. Independent of the meal_card bug — even if meal_card were fixed, shopping_list still crashes. Tests miss it because the test fixture aggregates an empty shopping_list (no 'items' in fixture days), so this loop body never runs. → _Fix:_ Change `item.total_g` to `item.total_grams` (line 14), OR rename the renderer key. Align the two; the renderer key total_grams is the source of truth.
- 🟠 **high** [test-gap] ✅verified→high `tests/test_pdf.py:100-119` — **PDF tests use an unrealistic nutrition fixture that hides both nutrition-render crashes**: _make_nutrition_plan() (lines 100-119) builds plan_json as a list of day dicts containing ONLY top-level macros (day/kcal/protein_g/fat_g/carb_g/fiber_g) with NO 'slots' key and therefore no per-slot 'items'. Real nutrition_service.generate (nutrition_service.py:142-176) emits days WITH 'slots', each slot WITH an 'items' list. Consequently: meal_plan.html.j2's `{% for slot in day_data.slots %}` iterates an Undefined (empty) loop so the meal_card crash never fires, and _build_nutrition_context aggregates an empty shopping_list so the shopping_list crash never fires. This is exactly why 230 tests pass while the live nutrition PDF is 100% broken — the templates are never exercised against production-shaped data. → _Fix:_ Replace the fixture's plan_json with the real nutrition_service shape (days with 'slots', each slot with an 'items' list of name/slug/category/grams dicts) and add a test that renders sections/meal_plan.html.j2 + sections/shopping_list.html.j2 end-to-end, asserting food rows and gram quantities appear. This would have caught both crashes.
- 🟡 **medium** [security] `app/adapters/pdf/renderer.py:48-54` — **Jinja autoescape is silently OFF for all .html.j2 templates (latent HTML injection)**: _make_jinja_env uses `select_autoescape(['html'])`, which enables autoescape only for filenames ending in `.html`. Every template here ends in `.html.j2`, so `'plan.html.j2'.endswith('.html')` is False and autoescape is OFF. Verified: `env.autoescape('plan.html.j2')` returns False. The renderer trusts this is safe because today only DB-sourced strings (exercise/food names) and the opaque client_id token reach templates. But profile.name (user-controlled Telegram first_name, set at intake bot.py:2030) is in-scope and any future template that renders a user free-text field (name, dislikes, custom food, coach feedback) would inject unescaped HTML into the PDF. This is a latent injection hole, not yet exploitable on the live path. → _Fix:_ Use a name function that matches the suffix: `select_autoescape(default_for_string=True, default=True)` or `autoescape=lambda name: True` for this HTML-only template set; or pass `select_autoescape(enabled_extensions=('j2','html'))`. Then audit any `| safe` usages (SVG/QR are intentional and stay).
- 🟡 **medium** [usability] `app/adapters/pdf/templates/sections/cover.html.j2:14` — **PDF addresses the client by opaque token (cl_a1b2c3...) instead of their name**: cover.html.j2 line 14 renders `Prepared for {{ client.client_id }}`, and base.html.j2 lines 14 & 5 put `client.client_id` in the running header and <title>. Since the Phase A refactor, client_id is an opaque `cl_<12 hex>` token (auth/roles.py:185 new_client_id), NOT a name. The client's real name lives in profile.name (set at intake, used everywhere else in the bot UI as `profile.name or profile.client_id`). So a paying client's professional PDF cover literally reads 'Prepared for cl_8f3a2b1c4d5e' — unprofessional and confusing on the primary deliverable. → _Fix:_ Render `{{ client.name or client.client_id }}` on the cover (line 14) and headers (base.html.j2 lines 5, 14). Note: client.name is user-controlled free-text, so fix the autoescape finding first to avoid introducing an injection vector.
- 🟡 **medium** [bug] `app/adapters/pdf/templates/sections/nutrition_overview.html.j2:9` — **Macro tiles and cover crash if any macro field is None (unguarded | round)**: Macro tiles (nutrition_overview.html.j2 lines 9, 16, 20, 24, 30) and the cover kcal tile (cover.html.j2 line 33) use `{{ nutrition_plan.X | round | int }}` with NO `or 0` guard. All these fields (kcal_target, protein_g, fat_g, carb_g, fiber_g) are `Optional[float] = None` on the model (models.py:208-213). Verified: `None | round` raises `TypeError: type NoneType doesn't define __round__ method`. Inconsistent with the 'Daily Targets' table immediately below (lines 46-66), which correctly guards every field with `or 0`. nutrition_service.generate always populates these, so the common path is safe, but any externally/partially created NutritionPlan with a null macro crashes the cover and overview sections. → _Fix:_ Add `or 0` to every unguarded macro expression, matching the Daily Targets table: e.g. `{{ (nutrition_plan.protein_g or 0) | round | int }}` on lines 9/16/20/24/30 and cover line 33.
- ⚪ **low** [content-gap] `app/adapters/pdf/renderer.py:164-168` — **Exercise QR/video codes are hardcoded empty — 'Video Demo' placeholder always shown**: _build_workout_context lines 164-168 set `slot['qr_data_uri'] = ''` for every slot with the comment 'Placeholder — real deployments would resolve exercise video URLs'. A working _make_qr_data_uri helper exists (lines 139-152) but is never called. So _exercise_card.html.j2 (lines 67-77) always renders the grey 'Video Demo' placeholder box and never an actual scannable QR. Every exercise in every delivered workout PDF has a dead, contentless video tile — a promised feature that never works. → _Fix:_ Resolve a real per-exercise video/demo URL (by exercise_id) and call `_make_qr_data_uri(url)` in the loop, or remove the QR column entirely so the layout doesn't reserve space for a permanently-empty tile.
- ⚪ **low** [bug] `app/bot.py:3537-3574` — **Nutrition plan marked active before Telegram delivery — no retry if send fails (plan can be silently undelivered)**: handle_nutrition_approve commits `plan.status='active'` and approved_at (lines 3537-3540) BEFORE attempting delivery. Delivery is `await context.bot.send_document(...)` (line 3569) with no try/except and no retry/queue. If the Telegram send throws (network, file too large, blocked bot), the plan is already persisted active, the admin's edit_message_text confirmation never updates, and there is no mechanism to re-attempt delivery — the client never receives the PDF and nothing flags it. (Contrast the workout path bot.py:3930-3948, which awaits send_document BEFORE _atomic_finalise_history, so a failure there leaves PendingApproval intact and recoverable.) Task's 'does the plan get lost?' answer: workout=recoverable, nutrition=can be silently lost. → _Fix:_ Wrap the nutrition send in try/except; on failure, do not present success, log/notify the admin, and either keep a 'pending_delivery' flag for retry or roll status back. Mirror the workout path's send-before-commit ordering.
- ⚪ **low** [bug] `app/services/email_service.py:28-33` — **send_plan: attachment hardcoded 'workout_plan.pdf', SMTP_USER fallback missing, and method is dead code**: send_plan (lines 8-45) is never called anywhere in the live app (verified: only references are its own definition and tests; live plan delivery is Telegram send_document). If revived: (1) attachment filename is hardcoded `workout_plan.pdf` (line 32) even though the method is generically a 'plan' sender — wrong for nutrition/combined PDFs; (2) it reads only `SMTP_EMAIL` (line 11) while the other three senders also accept `SMTP_USER` as fallback (lines 52, 90, 125) — inconsistent credential resolution; (3) subject/body hardcode 'Coach Shoaib' with no personalization of plan type. → _Fix:_ If keeping send_plan: parametrize the attachment filename, add `or os.getenv('SMTP_USER')` fallback to match the other senders, and parametrize subject/body. Otherwise delete it and its tests to remove dead, misleading code.
- ⚪ **low** [usability] `app/services/email_service.py:8-156` — **No recipient address validation; password-reset/coach-invite failures swallowed silently**: None of the four senders validate recipient_email format before handing it to smtplib (an empty or malformed address just produces a logged SMTP error). More importantly, callers ignore the boolean return: api/auth.py send_password_reset (line 149) and api/admin.py send_coach_invite (line 155) discard the result and respond {'ok': True} regardless. So a coach whose invite email bounces, or a user whose reset email fails, is told the operation succeeded while never receiving anything — a dead-end with no surfaced error or retry. (send_verification is explicitly fire-and-forget by design, which is acceptable.) → _Fix:_ Validate recipient with a simple regex / email-validator before sending. For coach-invite and password-reset, check the returned bool and surface a soft warning (or enqueue a retry) so the operator/user knows delivery is pending rather than guaranteed.

### LLM, checkin, audit services (`services-llm`)

_The LLM/check-in/audit subsystem is functional on the happy path but has several real defects. The most consequential: (1) check-in extraction failure silently advances the client's week and discards their telemetry with no user-facing error; (2) the bot's live check-in path passes exercise *names* where the extraction prompt and the (dead) CheckInService both require canonical exercise *ids*, so the LLM can almost never map feedback to a planned slot, neutering autoregulation; (3) LLM model-id defaults diverge across the codebase and the fallback id (gemini-2.5-flash) is not present in OpenRouter's current catalog while the settings default (gemini-3.1-flash-lite-preview) is, so the bot's no-arg/_make_llm_client paths fail when LLM_MODEL_ID is unset; (4) coach-edit LLM output is only JSON-syntax-validated, never schema/semantically validated against the exercise DB or volume caps, so a malformed-but-valid edit can corrupt an active plan (FastAPI coach path writes it entirely unvalidated). CheckInService (my assigned file) is dead code — no caller in app/ or tests/; the bot reimplements the flow inline and reintroduces the catalog bug the service avoids. audit.py is small and mostly clean. There is no diet/nutrition logic in these services (handled elsewhere), and there is no numeric-pain-to-severity mapping rule, leaving pain triage to LLM guesswork._

**Coverage:** Covered all assigned files: llm_service.py, checkin_service.py, audit.py, adapters/llm/openrouter.py, adapters/llm/extractors.py, domain/checkin/schema.py, plus the live callers in bot.py, routes.py, api/coach.py and the autoregulation/prompt files they depend on. VERIFIED: model ids checked against OpenRouter's live /models endpoint (gemini-3.1-flash-lite-preview EXISTS, gemini-2.5-flash does NOT as of 2026-06-06 — single-source, treat with moderate confidence). CheckInService confirmed to have zero callers (dead code). audit.py is functionally clean (only low note: log_audit calls session.commit(), which commits any other unflushed work in the caller's shared session, and a swallowed commit failure leaves the session needing rollback). MISSING for a real product: no numeric-pain (e.g. "7/10 pain") → categorical severity mapping rule in the extract prompt, so severity triage is arbitrary; no retry/escalation actually wired despite extractors.py docstring promising "Escalate to gemini-2.5-pro on retry failure" (never implemented); no guard that LLM-returned exercise_ids exist in the catalog or that edited plans respect volume/fatigue caps; no diet-type/allergen handling in these services (nutrition is a separate subsystem). Could not exhaustively trace every bot conversation-handler edge for the None-extraction path beyond the function read.

- 🟠 **high** [correctness] ✅verified→high `app/bot.py:2736-2790` — **Check-in extraction failure silently advances the week and discards the client's telemetry with no error shown**: In _finalize_checkin, when extract_checkin raises (e.g. transient OpenRouter outage), the except at line 2739-2741 sets `extraction = None` and swallows the error (only logs a warning). Execution falls through: the telemetry write-back is skipped because line 2767 guards on `if extraction and extraction.exercises:`, autoregulation is skipped (line 2797 `if extraction is not None:`), but line 2788 still runs `client.week_number += 1` and commits, and run_generation_and_dispatch is called at 2863 to produce a fresh plan. The user is never told their check-in failed. Net effect: the client's reported loads/RPE for the week are lost, the week counter advances so they cannot re-check-in for that week, and the next plan is generated as a normal progression with zero feedback incorporated. → _Fix:_ If `extraction is None`, send the user an explicit failure message and `return` WITHOUT incrementing week_number or dispatching a new plan, so they can retry /checkin. Only advance the week once extraction (or at least the telemetry write) succeeded.
- 🟠 **high** [bug] ✅verified→high `app/bot.py:2431-2436` — **Bot passes exercise NAMES as the lift catalog, but the extraction prompt/schema require canonical exercise_ids**: The bot builds `checkin_lift_catalog` from `slot.exercise_name` (display strings like 'Barbell Back Squat') at lines 2431-2436, and passes it to extract_checkin (bot.py:2738). But prompts/checkin_extract.j2 line 23-24 presents this list as '## Available exercise catalog (canonical IDs)' and CheckInExtraction.exercise_canonical must be a catalog exercise_id (schema.py:30). The autoregulation match in derive_plan_delta/_find_slot keys on `slot.exercise_id` (autoregulation.py:118,183). So the LLM is shown names, told they are ids, and must emit ids that match exercise_id — a type mismatch that makes exercise_canonical almost never line up with a real slot, so per-exercise RPE-error autoregulation (Rule 4) and telemetry write-back (bot.py:2774) rarely fire. CheckInService.ingest does this correctly with `ex['exercise_id']` (checkin_service.py:32), but that service is never called. → _Fix:_ Build the catalog from exercise_id (as CheckInService.ingest does), or pass a name→id map and resolve names to ids after extraction. Align the bot path with the service.
- 🟠 **high** [bug] ❌REFUTED `app/settings.py:33` — **Divergent LLM model-id defaults; the fallback id (gemini-2.5-flash) is absent from OpenRouter's current catalog**: Three different default model ids coexist: settings.py:33 / container.py:24 / .env.example use 'google/gemini-3.1-flash-lite-preview'; llm_service.py:33 fallback and bot.py:2378 (_make_llm_client) use 'google/gemini-2.5-flash'; extractors.py docstring (line 8) claims 'google/gemini-2.5-flash'. The committed .env sets OPENROUTER_API_KEY but NOT LLM_MODEL_ID, so at runtime: the FastAPI/container path uses the settings default, while every bot path (FlashCommunicationService() no-arg, _make_llm_client) falls back to gemini-2.5-flash. A live query of OpenRouter /models (2026-06-06) returned gemini-3.1-flash-lite-preview as present and gemini-2.5-flash as NOT present — meaning the bot's fallback id would 404/400 from OpenRouter whenever LLM_MODEL_ID is unset, taking down coach-message generation, coach-edits, FAQ, exercise tips, and check-in extraction. The container path and the bot path also run two different models, so behavior is inconsistent between FastAPI and Telegram. → _Fix:_ Single-source the model id (have _make_llm_client and the llm_service fallback read settings.llm_model_id), set LLM_MODEL_ID in .env, and update the extractors.py docstring. Confirm the chosen id against the live OpenRouter model list at deploy time.
- 🟠 **high** [correctness] ✅verified→high `app/api/coach.py:204-221` — **FastAPI coach-edit writes LLM output to the active plan with no WorkoutWeek schema validation**: edit_plan calls apply_coach_edits (line 204), which only validates JSON *syntax* (llm_service.py:124 `json.loads(content)`), not the WorkoutWeek structure. The result is written straight to pending.workout_json at line 217 with no WorkoutWeek.model_validate_json. approve_plan then does `workout = json.loads(pending.workout_json)` (coach.py:153) and persists it to WorkoutHistory (line 162) — again no schema validation. So a schema-valid-JSON-but-wrong-shape LLM edit (e.g. `{}`, missing `days`, dropped slots) silently corrupts the client's active plan; the corruption only surfaces later when something tries to load it as a WorkoutWeek. The bot path (bot.py:4018) at least calls WorkoutWeek.model_validate_json before saving; the FastAPI path does not. → _Fix:_ After apply_coach_edits, run WorkoutWeek.model_validate_json on the result before assigning to pending.workout_json; on ValidationError return 422 and leave the prior plan intact.
- 🟡 **medium** [test-gap] `app/services/checkin_service.py:28-119` — **CheckInService is dead code — no caller in app/ or tests/; the live bot path reimplements (and breaks) the flow**: grep for `CheckInService(` across app/ and tests/ returns zero instantiations; the only references to the class name are its own definition. The bot calls extract_checkin/render_digest and derive_plan_delta/apply_delta directly (bot.py:2738, 2809, 2798) instead of going through this service. This matters because the service does the catalog correctly (exercise_id at line 32) while the live bot path does not (see the name/id bug). So the well-formed orchestration exists but is unused, and the buggy reimplementation is what actually runs. apply_autoregulation/request_clarifications are also never exercised in production. → _Fix:_ Either route the bot check-in flow through CheckInService (fixing the catalog bug for free) or delete the service to avoid a false sense of coverage. Add an integration test that drives the real check-in path end-to-end.
- 🟡 **medium** [correctness] `app/services/llm_service.py:85-128` — **Coach-edit validation checks JSON syntax only; admin free-text is an untrusted prompt-injection path into the plan with no semantic guard**: apply_coach_edits validates only `json.loads(content)` (line 124) before returning. Even the bot path's downstream WorkoutWeek.model_validate_json (bot.py:4018) checks structure, not semantics: nothing verifies that returned exercise_ids exist in the exercise DB, that days/slots weren't dropped, or that target_weight/rpe/sets stay within the engine's volume_cap / MAX_FATIGUE / RPE bounds. The coach feedback is free text inserted verbatim into the user prompt (line 97-100), so an instruction like 'replace all lifts with X' or an injected 'ignore previous rules' is applied with no deterministic backstop. This defeats the 'deterministic engine, LLM only formats' design constraint at the edit step. → _Fix:_ After the JSON parse, validate the edited WorkoutWeek against the catalog (every slot.exercise_id must exist) and re-run the volume/fatigue/RPE invariants; reject edits that violate them. Treat coach_feedback as data, not instructions (e.g. clearly delimit it and instruct the model to ignore embedded directives).
- 🟡 **medium** [bug] `app/services/llm_service.py:113` — **complete() can return None; content.strip() then raises AttributeError**: OpenRouterClient.complete returns `response.choices[0].message.content` (openrouter.py:44), which the OpenAI SDK types as Optional[str] — it is None when the model returns a tool/function call, an empty/filtered completion, or a refusal with no content. apply_coach_edits does `content = content.strip()` at line 113 with no None guard, raising AttributeError instead of the intended clean ValueError('LLM returned invalid JSON'). generate_coaching_message (line 66) and generate_exercise_tips (line 83) return the raw value, so a None can propagate into the email/PDF body and the Telegram reply. The bot's FAQ path guards this with `(answer or '')` (bot.py:845); the service methods do not. → _Fix:_ In OpenRouterClient.complete, return `response.choices[0].message.content or ''` (or raise a typed error on None). At minimum guard `content = (content or '').strip()` in apply_coach_edits.
- ⚪ **low** [bug] `app/bot.py:2826-2843` — **RPE-jump action badge branch is dead — new_avg_rpe_badge is set equal to prior_avg_rpe**: Line 2832 sets `new_avg_rpe_badge = prior_avg_rpe  # approximation for jump detection`, then line 2838-2842 fires the red badge only if `new_avg_rpe_badge - prior_avg_rpe > 1.5`. Since the two values are identical by construction, the difference is always 0.0 and the condition is unreachable, so the RPE-spike (red) badge for high reported RPE never triggers; such check-ins fall through to the yellow default at line 2845. The admin loses the intended high-RPE escalation signal. → _Fix:_ Compute new_avg_rpe_badge from an independent quantity (e.g. the plan's target average RPE) so the comparison is meaningful, or remove the dead branch and badge directly off prior_avg_rpe against the planned RPE.
- ⚪ **low** [data-quality] `app/adapters/llm/extractors.py:1-92` — **Docstring promises pro-model escalation and Instructor that the code does not implement**: The module docstring (lines 7-9) states 'Provider: OpenRouter → google/gemini-2.5-flash / Escalate to google/gemini-2.5-pro on Instructor retry failure' and line 5 says 'structured JSON via Instructor'. The actual extract_checkin uses a single hardcoded LLMClient with manual json.loads + model_validate and no model escalation and no Instructor library — it just retries the same model 3x (plus a redundant tenacity @retry wrapper at line 37 around an already-internal 3-attempt loop at line 61, giving up to 9 attempts and confusing the failure semantics). Misleading docs plus the double-retry are a maintenance/operability hazard. → _Fix:_ Update the docstring to match reality (no Instructor, no pro escalation), and remove either the tenacity decorator or the inner loop so retry count is single-sourced. If pro-escalation is desired, implement it.
- ⚪ **low** [content-gap] `prompts/checkin_extract.j2:14-16` — **No numeric-pain → categorical severity mapping; pain triage is left to LLM guesswork**: PainFlag.severity is a categorical enum none/mild/moderate/severe/unknown (schema.py:21), and needs_coach_review plus the reactive-deload safety rule key off 'moderate'/'severe' (schema.py:115, autoregulation.py:54,108). But the extract prompt gives a precise RPE↔RIR table (lines 8-9) and yet NO rule for converting a numeric pain rating to severity. Few-shot Example C (line 136) has the client say 'Rated it 7/10 pain' and the expected output is severity:'severe' with no stated basis, so the model has no consistent threshold (is 5/10 moderate or severe?). Inconsistent pain classification directly affects the safety-critical coach-review and deload triggers. → _Fix:_ Add an explicit pain-scale mapping to the prompt (e.g. 1-3 mild, 4-6 moderate, 7-10 severe) so numeric pain reports map deterministically to the severities the safety rules depend on.
- ⚪ **low** [correctness] `app/services/audit.py:33-34` — **log_audit commits the caller's shared session and can leave it in a broken state on failure**: log_audit calls session.add(row) then session.commit() (lines 33-34) on the session passed in by the caller (e.g. api/admin.py:156, 212, 231 pass the request's db session). committing here also flushes/commits any other unflushed work the caller had pending in that same session — auditing is not supposed to control transaction boundaries. If the commit raises (e.g. integrity error on AuditEvent), the except at line 35 only logs a warning and swallows it, but the session is now in a failed/needs-rollback state, so the caller's subsequent use of the same session can raise PendingRollbackError. Auditing 'must not break the request' (docstring) but this can. → _Fix:_ Use a separate short-lived session for the audit insert, or flush-only and let the caller own the commit; on failure call session.rollback() before swallowing so the caller's session stays usable.

### Flutter mobile app (`mobile`)

_Stale backend contracts; data silently wrong._

**Coverage:** Missing nutrition inputs and injury handling.

- 🟠 **high** [correctness] ✅verified→high `mobile/lib/features/coach/coach_review_screen.dart:349` — **Coach review exercise names all '?'**: Reads slot['exercise']['name'] but slots are flat exercise_name. → _Fix:_ Use slot['exercise_name'].

### Planted-bug / obfuscation sweep (`integrity-sweep`)

_Integrity sweep: one planted obfuscation found at app/domain/nutrition/food_db.py line 18. Everything else clean._

**Coverage:** Targets planted obfuscations only. String-slice trick: one at food_db.py:18. No eval/exec, base64, bare except, TODO markers, mutable defaults, or committed creds.

- 🟡 **medium** [data-quality] `app/domain/nutrition/food_db.py:18` — **Planted string-slice obfuscation builds garbage diet tag egan**: First diet_tag is vegan[1:]+egan[1:][:0] which evaluates to egan. Sibling chicken_thigh is clean omnivore/keto/gluten_free. egan is the sole singleton in FOOD_DB; only used as a membership test at meal_builder.py:105, so chicken keeps valid tags and stays selectable. Data-integrity damage. Also note JWT default secret change-me-in-production is warn-only at settings.py:58 (security, medium). → _Fix:_ use plain literal omnivore/keto/gluten_free and add a CI vocabulary assert

### Test suite gaps (`tests-coverage`)

_test suite gaps_

**Coverage:** see findings

- 🔴 **critical** [test-gap] ✅verified→critical `app/domain/nutrition/meal_builder.py:109-114` — **Medical-condition filtering untested; produces EMPTY pool for diabetic/hypertensive clients**: filter_food_pool requires low_sugar or low_sodium in f.medical_tags but all 68 foods in food_db have empty medical_tags. Verified live: filter_food_pool with medical_conditions type2_diabetes returns 0 foods and hypertension returns 0. nutrition_service.py:93 passes profile.medical_conditions straight through, so a diabetic or hypertensive client's plan is built from an empty pool and build_day_plan falls back to an empty selection yielding total kcal 0. No test exercises this path. → _Fix:_ Add tests asserting filter_food_pool with type2_diabetes and hypertension returns a non-empty pool and that build_day_plan over it yields kcal above 0 passing validate_day; populate food_db medical_tags or relax the filter to read sodium and sugar columns.
- 🟠 **high** [test-gap] ✅verified→critical `app/domain/nutrition/meal_builder.py:102-103` — **Religious-restriction filtering untested; halal/kosher filter is dead and leaks pork and shellfish**: filter_food_pool removes foods whose religious_restrictions intersect the client's, but all 68 foods have empty religious_restrictions, so it never removes anything. Verified live: filter_food_pool with religious_restrictions halal returns all 68 foods including pork_tenderloin, lean_beef, beef_sirloin and shrimp. nutrition_service.py:90 wires profile.religious_restrictions in, so a halal or kosher client is served forbidden foods. No test asserts religious filtering removes anything. → _Fix:_ Add a test asserting filter_food_pool with halal excludes pork_tenderloin and that at least one food carries a religious tag; it fails today, forcing food_db to tag non-halal and non-kosher items.
- 🟠 **high** [test-gap] ✅verified→high `tests/test_generator.py:15-188` — **Generator never swept across all avatar by days combos; untested combos emit EMPTY training days**: Tests cover only powerbuilder-4, powerlifter-4, gen_pop-3 and gen_pop-5. Sweeping all 21 cells through generate reveals: powerlifter-6 (a push pull legs split) produces an EMPTY Pull day with 0 slots because the powerlifter exercise pool cannot fill a pull day; and all three avatars at days 7 produce an EMPTY Training Day 7 from the else branch of _resolve_split at generator.py lines 97 to 98. A client picking 6 days as a powerlifter, or 7 days for any avatar, receives a blank workout day. → _Fix:_ Add a parametrized test over powerlifter, powerbuilder and gen_pop times days 1 through 7 asserting every generated day has at least one slot, or that unsupported day counts raise a clear error; it fails on powerlifter-6 and all avatars at 7.
- 🟠 **high** [test-gap] ✅verified→high `app/services/nutrition_service.py:85-115` — **Nutrition orchestrator (BMR to TDEE to macros to 7 day meal build to validate) has zero tests**: Lines 85 to 115 wire the entire nutrition pipeline end to end (energy plus macros plus filter_food_pool plus seven build_day_plan calls plus persistence) and are the production entry point. No test references nutrition_service. test_nutrition.py only unit-tests energy.py and macros.py in isolation; the meal building half and the integration never run, so the empty-pool failures, the never-called validate_day, and any 0 kcal output reach production untested. An end to end probe over the food db for a healthy omnivore does validate (kcal 2403 versus target 2400, validate_day returns no errors), proving an integration test is feasible and cheap. → _Fix:_ Add an integration test calling the public nutrition generation function for a healthy omnivore (7 days, each passing validate_day), a vegan, and a diabetic (assert non-empty and kcal above 0, currently fails), using an in-memory SQLite session like conftest.
- 🟡 **medium** [bug] `app/adapters/pdf/renderer.py:196` — **Day-summary main-lift selector uses stale slot_type main_lift the generator never emits; PDF fixtures hide it**: renderer.py line 196 selects the main lift as the first slot whose slot_type equals main_lift, else slots index 0. The live generator emits slot_types main_compound, secondary_compound and isolation (verified) and never main_lift, so on every real plan the predicate never matches and the code silently falls through to slots index 0; the labelled main lift in the PDF day summary is not guaranteed to be the main compound. The PDF tests pass only because test_pdf.py lines 52 and 71 hardcode the stale main_lift and primary_accessory vocabulary, validating the renderer against data the generator never produces. → _Fix:_ Change renderer.py line 196 to compare slot_type to main_compound, then rebuild PDF fixtures from WorkoutGenerator generate output so fixtures cannot drift from production output.
- 🟡 **medium** [test-gap] `tests/test_generator.py:n/a` — **AutoRegulator.calculate_next_load (live next-week weight formula) untested at RPE extremes**: generator.py line 364 calls AutoRegulator.calculate_next_load to compute next week target_weight from prior telemetry (4 percent RPE error correction plus 2.5 percent stimulus jump, rounded to 2.5 kg). No test references AutoRegulator or calculate_next_load. Probing extremes: actual RPE 10 versus target 6 gives 85 kg from 100 kg; actual 5 versus target 9 gives 115 kg; a nonsense target 1 with actual 10 gives 65 kg. There is no clamp on pathological inputs, and a regression here silently prescribes wrong weights to every returning client. The parallel derive_plan_delta and apply_delta system is tested, which masks that this distinct, also live formula is not. → _Fix:_ Add unit tests for calculate_next_load: monotonicity (higher actual RPE lowers next weight), the deload case (lower next target RPE drops weight), 2.5 kg rounding, and a sanity clamp; add an input range guard if extremes produce absurd loads.
- 🟡 **medium** [test-gap] `tests/test_checkin.py:106-118` — **Deload asserted triggered but never asserted to change the next plan; effectiveness untested**: The deload test asserts only that delta.trigger_deload is True. apply_delta at autoregulation.py lines 149 to 175 iterates only slot_adjustments, so a deload-only delta (trigger_deload True with no slot_adjustments) is a no op there. The actual deload effect flows through bot.py lines 2798 to 2799 (force_deload equals delta.trigger_deload) into a re generation that no test exercises. Nothing asserts a triggered deload actually lowers RPE or volume in the regenerated week. checkin_service.py also has zero tests. → _Fix:_ Add a test feeding high fatigue plus poor sleep telemetry through the real check-in to regeneration path (checkin_service.apply_autoregulation or the bot handler) and asserting next week RPE and volume drop versus the non-deload baseline; also cover checkin_service.py.
- 🟡 **medium** [test-gap] `app/api/sets.py:26-46` — **log_set writes a SetLog against an unvalidated history_id with no ownership check and no IDOR test**: log_set takes history_id in the request body and persists a SetLog with client_id equal to the authenticated user but history_id equal to the body value without verifying that history_id belongs to the user or even exists. A client can attach set logs to an arbitrary or another user's WorkoutHistory id. The read sibling list_sets_for_history correctly filters by client_id, but the write path is unguarded. test_mobile_api.py line 249 checks only a happy path insert; there is no cross user or IDOR test anywhere for the web API. → _Fix:_ Validate history_id ownership in log_set (select WorkoutHistory where history_id equals the body value and client_id equals the user, 404 if absent). Add an IDOR test: user A logs a set with user B's history_id and expects 404 or 403, plus a test that user A's plans and sets never return user B's rows.
- 🟡 **medium** [test-gap] `tests/test_pdf.py:43-97` — **PDF renderer never tested with sparse, empty, or malformed data**: All PDF tests use a fully populated single day fixture. There is no test for a WorkoutHistory whose workout_json has empty days or a day with empty slots; a non JSON or malformed workout_json (renderer.py line 159 does a bare json.loads that would raise inside _build_workout_context); or a nutrition day dict missing macro keys. renderer.py lines 111 and 180 use a sets subscript rather than get inside sums, so a slot lacking the sets key would raise KeyError. These are exactly the regressions a sparse data test would catch. → _Fix:_ Add tests rendering an empty days list, a day with zero slots, a slot dict missing sets, and malformed workout_json (assert a controlled error not an uncaught 500). Harden renderer.py lines 111 and 180 to use get with a default of 0.
- ⚪ **low** [data-quality] `app/domain/nutrition/food_db.py:18` — **chicken_breast diet_tags first element is the malformed string egan (typo expression), uncaught by any test**: Line 18 builds the diet tag list with a string slicing expression that evaluates to the string egan (verified), so chicken_breast diet_tags is egan, omnivore, keto, gluten_free. It does not make chicken vegan (nothing filters on egan and the real vegan tag is absent), so the impact is cosmetic data integrity rather than wrong output, but it is emblematic: a single food db integrity test that every diet tag is in a known vocabulary would have flagged it instantly, and food_db.py has no such test. → _Fix:_ Replace the expression with the literal omnivore (or vegan if intended) and add a food db integrity test asserting every diet tag, allergen and medical tag belongs to a fixed allowed vocabulary and that there are no duplicate slugs.
- ⚪ **low** [test-gap] `app/domain/nutrition/meal_builder.py:202-217` — **Naive equal-split portion fallback (used when PuLP is absent) is never exercised**: _optimise_portions falls back to _naive_portions when PuLP is unavailable (lines 147 to 151). PuLP is installed in this environment, so the equal-split fallback never runs in tests, while the PuLP path itself also has no direct test. If a deploy ships without PuLP, meal portioning silently switches to a much cruder equal split that no test validates and that could produce days failing validate_day. The zero kcal food guard at line 211 and the 30 to 400 gram clamp are untested. → _Fix:_ Add a test that monkeypatches the pulp import to None and asserts _optimise_portions still returns sane clamped portions; plus a direct test of _optimise_portions hitting macro targets within tolerance when PuLP is present.
- ⚪ **low** [test-gap] `tests/test_nutrition.py:n/a` — **No diet-type or allergen edge-case tests for filter_food_pool**: filter_food_pool hard cascade (allergens; the diet_type omnivore, balanced and none bypass at line 104; dislikes matching on slug or lowercased name at line 108) has no tests. Edge cases that would catch regressions: a vegan filter must drop all animal foods; a multi allergen client (nuts plus dairy plus gluten) must not be served any matching food; the diet_type bypass values must not over filter; dislikes given as a display name versus slug must both match. None are tested. → _Fix:_ Add parametrized tests over diet_type vegan, vegetarian, keto and pescatarian asserting the returned pool diet tags all contain the requested type; a multi allergen test asserting zero matching allergens remain; a dislikes test matching by both slug and name.
